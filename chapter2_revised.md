# 第2章 训练后量化的难点与基准

## 2.1 引言

模型量化根据是否需要对原始模型权重进行重训练，可分为**训练后量化**（Post-Training Quantization, PTQ）[1][2][3]与**量化感知训练**（Quantization-Aware Training, QAT）[4][5][6]两大范式。第 1 章已对 QAT 的基本流程、伪量化节点机制以及直通估计器（STE）下的梯度反传与权重振荡问题做了完整介绍。本章不再重复这一线性量化与 STE 推导的共性内容，而是将焦点收敛到**与 PTQ 范式紧密相关的特有难点与基准方法**之上。

PTQ 与 QAT 的本质区别在于数据使用方式与优化空间：QAT 需要完整训练集与全部标签，对网络权重进行端到端重训练以"逼近"量化分布；PTQ 则仅依赖少量校准数据（通常为训练集的极小子集），通过分布统计与有限的局部参数微调，将一个已经收敛的全精度模型"快速折叠"为低比特量化模型。这一根本性的设计选择使 PTQ 在数据隐私敏感（医疗、金融）、模型迭代频繁、计算资源受限的工业部署场景下具有 QAT 难以替代的实用价值，但同时也带来了 QAT 不会遇到的若干特有难点：

- **优化空间窄**：PTQ 不能像 QAT 那样依赖完整数据集对权重进行任意更新，其优化必须严格控制在"重构误差最小化"这一狭窄空间之内。
- **数据信号弱**：仅有数百到数千张校准样本，统计量与分布特性容易偏离全集，校准集的代表性直接决定 PTQ 的精度上限。
- **几何约束硬**：PTQ 不修改全精度权重的整体结构，因此预训练模型的损失景观几何特性（极小值的"宽窄"）成为 PTQ 无法突破的硬天花板。

正是上述三类难点共同导致了 PTQ 在 4 比特及以下场景下的"精度断崖"。本文第 3、4 章正是分别针对其中的"几何约束"与"数据信号"两条主线，提出对应的解决方案。

本章作为后续章节的理论基础，结构安排如下：2.2 节简要回顾本章后续推导所必需的量化基本概念（与第 1 章重复部分一律以引用方式处理）；2.3 节详细介绍 PTQ 的三阶段标准流程；2.4 节系统剖析 PTQ 在极低比特下的四类特有难点；2.5 节介绍本文采用的 PTQ 基准方法及其相互关系；2.6 节对全章进行回顾与总结。

---

## 2.2 PTQ 视角下的量化基本概念

第 1 章已完整给出线性量化的数学定义、对称/非对称量化、均匀/非均匀量化以及舍入/截断误差的理论分析。本节仅就**PTQ 推导后续将反复引用**的若干核心约定进行最小必要的回顾，公式编号与第 1 章保持独立。

### 2.2.1 量化映射与量化参数

针对一个预训练 FP32 神经网络模型 $f(\cdot)$，记其待量化权重集合为 $W = \{w_1, \dots, w_N\}$，待量化激活集合为 $A = \{a_1, \dots, a_M\}$，所有量化映射构成集合 $\mathcal{F} = \{q_1(\cdot), \dots, q_{N+M}(\cdot)\}$，得到量化模型 $f_q(\cdot)$。PTQ 的形式化目标为在保留原始任务损失的前提下使全精度与量化模型间的损失差异最小：

$$
\min_{\mathcal{F}}\ \big| L(f(\cdot)) - L(f_q(\cdot)) \big|.
\tag{2-1}
$$

线性均匀量化对单个张量 $x$（权重或激活）的量化与反量化分别由量化参数 $(s, z)$ 决定：

$$
x_{\text{int}} = \text{clip}\!\left(\left\lfloor \tfrac{x}{s} \right\rceil + z,\ a,\ b\right), \qquad
\hat{x} = (x_{\text{int}} - z)\cdot s,
\tag{2-2}
$$

其中 $s$ 为量化步长（scale），$z$ 为零点（zero-point），$\lfloor\cdot\rceil$ 为四舍五入算子，$[a,b]$ 为目标量化整数区间。对称量化对应 $z=0$，非对称量化引入 $z\neq 0$ 以适应非零均值分布。在不引起歧义时，下文将"量化-反量化"复合操作简称为量化操作 $q(\cdot)$。

### 2.2.2 量化误差的关键性质：步长 $s$ 的对偶角色

第 1 章详细推导了舍入误差与截断误差的数学性质，本章仅强调一条对 PTQ 设计极为关键的结论：

**步长 $s$ 同时决定两类误差的方向，且方向相反**——

| 误差类型 | 反量化后的量级 | 与 $s$ 的关系 |
|---|---|---|
| 舍入误差 $\varepsilon_r$ | $|\varepsilon_r| \le s/2$ | **正比** |
| 截断误差 $\varepsilon_c$ | 由分布尾部与边界距离决定 | **反比** |

记联合量化均方误差为：

$$
\text{MSE}(s) = \mathbb{E}_x\!\left[(x - q(x))^2\right] = \mathbb{E}[\varepsilon_r^{2}] + \mathbb{E}[\varepsilon_c^{2}],
\tag{2-3}
$$

则存在唯一的最优步长 $s^{*}$ 使得 $\text{MSE}(s)$ 取得最小：

$$
s^{*} = \arg\min_{s}\ \big\{\mathbb{E}[\varepsilon_r^{2}(s)] + \mathbb{E}[\varepsilon_c^{2}(s)]\big\}.
\tag{2-4}
$$

公式 (2-4) 揭示了 PTQ 步长优化的本质：**所有 PTQ 算法（无论朴素与否）的核心任务都是在舍入误差与截断误差之间寻找针对当前数据分布的最优平衡点**。这一对偶性既是 PTQ 算法设计的出发点，也是后续 2.4 节分析 PTQ 难点的关键线索。

---

## 2.3 训练后量化的标准流程

PTQ 是一套以"少量数据 + 离线优化"为核心特征的量化方法论，其完整流程可标准化为三个阶段：**校准阶段**（Calibration）、**重构阶段**（Reconstruction）与**导出部署阶段**（Export & Deployment）。三阶段的协同共同决定了 PTQ 模型的最终精度与部署效率。整体流程如图 2-1 所示。

**【图片占位区：图 2-1 PTQ 三阶段标准流程示意图（校准 → 重构 → 导出部署）】**

**Fig. 2-1 The standard three-stage pipeline of PTQ (Calibration → Reconstruction → Export).**

### 2.3.1 校准阶段：量化参数的统计初始化

校准阶段的目标是利用一个小规模校准集 $\mathcal{D}_c$（通常为训练集中随机或精心采样的 100~1024 张样本）对全精度模型执行一次前向推理，收集各层激活与权重的分布统计量（最大值、最小值、均值、方差等），并以此初始化每层的量化参数 $(s, z)$。

主流的步长初始化策略包括以下三种：

**（1）最大值初始化（MinMax）**：直接以分布的极值确定量化范围

$$
s_{\text{MinMax}} = \frac{x_{\max} - x_{\min}}{2^q - 1}.
\tag{2-5}
$$

实现最简单，但对离群点（outlier）极度敏感——单个异常激活就可能将步长拉伸到正常信号的几十倍以上，导致绝大多数样本被压缩至极少量化格点上，等效精度急剧下降。

**（2）均方误差初始化（MSE）**：通过网格搜索或解析方法直接最小化公式 (2-3) 的 $\text{MSE}(s)$，对离群点鲁棒性显著优于 MinMax。

**（3）百分位初始化（Percentile）**：以分布的 99.9% 或 99.99% 分位数代替最大值作为量化边界，主动裁剪长尾离群点，在激活分布存在重尾的场景下尤其有效。

对于朴素 PTQ，校准阶段结束后即可直接导出量化模型；对于先进 PTQ，校准结果仅作为下一阶段重构优化的**初始化起点**。

### 2.3.2 重构阶段：基于校准集的局部参数优化

为弥补朴素 PTQ 在低比特下的精度损失，先进 PTQ 方法引入重构阶段，以**最小化全精度与量化网络在层级或块级输出之间的均方误差**为目标，对量化参数（甚至权重的舍入决策、量化步长本身）进行有限的梯度优化。以逐层重构为例，第 $l$ 层的重构目标为：

$$
\min_{s^{l}, z^{l}}\ \mathbb{E}_{x\in\mathcal{D}_c}\!\left[\big\| y^{l} - \hat{y}^{l} \big\|_{F}^{2}\right],
\tag{2-6}
$$

其中 $y^{l}$ 与 $\hat{y}^{l}$ 分别为第 $l$ 层在全精度与量化下的输出。由于重构目标涉及不可微的四舍五入算子，梯度反传同样需要借助直通估计器（STE，详见第 1 章 2.4 节），STE 在 PTQ 重构中产生的特有问题将在本章 2.4.1 节专门分析。

不同 PTQ 方法在重构阶段的差异主要体现在三个维度：**重构粒度**（逐层 vs. 逐块）、**重构对象**（仅权重舍入 vs. 同时优化激活的量化参数）以及**额外正则化机制**（Fisher 信息加权、随机激活丢弃等）。这些差异将在 2.5 节展开。

### 2.3.3 导出部署阶段

重构完成后，PTQ 将优化后的量化参数 $(s, z)$ 与全精度权重结合，将权重转换为真实的低比特定点整数并导出量化模型。在硬件推理时，低比特权重由专用计算单元（INT8/INT4 核心）执行卷积或矩阵乘法，累加器以高位整数（典型为 INT32）保存中间结果，最终通过**再量化**（Re-quantization）将累加器输出转换回目标低比特格式作为下一层输入。这一推理流程与 QAT 部署阶段完全一致，PTQ 的额外开销仅体现在校准与重构的离线时段，量化模型本身在硬件上的延迟、功耗、内存占用与 QAT 模型无差异。

PTQ 相较 QAT 的核心优势在于：**无需完整训练集**（仅需少量校准数据）、**无需端到端重训练**（整个流程通常在数分钟至数小时内完成）。这一特性使 PTQ 在数据隐私敏感（医疗、金融）、模型快速迭代、算力受限的工业部署中拥有不可替代的实用价值，也正是本文聚焦 PTQ 的根本动机。

---

## 2.4 训练后量化的特有难点

PTQ 的高效性背后是优化空间窄、数据信号弱、几何约束硬三重根本性挑战。本节从四个相互关联的维度系统剖析制约 PTQ 极低比特精度的特有瓶颈，前三类难点对应 PTQ 算法本身的"内部局限"，第四类难点则指向 PTQ 范式之外、由全精度模型几何特性所设的"外部天花板"，并直接构成本文第 3 章研究的出发点。

### 2.4.1 STE 在 PTQ 重构中的梯度估计偏差

PTQ 重构阶段（公式 2-6）需要对量化参数进行梯度优化，而四舍五入算子的导数几乎处处为零。第 1 章已经详细给出了 STE 的形式定义、对量化函数 $q(\cdot)$ 在量化范围内梯度为 1、范围外梯度为 0 的推导（第 1 章公式 (2-7)~(2-8)），以及在 QAT 设定下基于 OOQ 一维回归模型的权重振荡分析（第 1 章公式 (2-9)~(2-14)）。本节不再重复推导，仅强调 STE 在 **PTQ 设定**下产生的两个特有后果：

**（1）有偏估计放大单步优化误差**：QAT 拥有完整训练集，可通过大量迭代"平均掉" STE 的梯度偏差。而 PTQ 仅有数百到数千张校准样本与有限迭代步数，STE 的有偏梯度无法被充分稀释，单步优化方向的偏差更直接地反映在最终量化参数上。

**（2）量化边界振荡损害校准集泛化性**：根据第 1 章公式 (2-12)，权重在量化边界 $\bar{w}$ 两侧的梯度方向相反、绝对值之比为 $k = s/d - 1$。在 PTQ 重构阶段，受限于校准集规模，振荡轨迹无法收敛到稳定均值，最终残留振荡相位极易被校准集的样本偏差选择，进一步放大"校准集精度高、测试集精度跌"的过拟合现象。

上述两点共同导致：PTQ 的重构优化必须设计额外机制（如下文将介绍的随机丢弃、块级 Fisher 加权等）来主动抑制 STE 偏差的累积效应，单纯依赖 STE + SGD 的朴素方案在极低比特下会迅速失效。

### 2.4.2 量化误差在层间的累积传播

PTQ 不修改全精度权重的整体结构，各层的量化误差不相互独立，而是沿前向传播方向逐层放大并累积。设第 $l$ 层的全精度输出为 $y^{l}$，量化输出为 $\hat{y}^{l}$，则第 $l$ 层引入的扰动为 $\delta^{l} = \hat{y}^{l} - y^{l}$。该扰动作为第 $l+1$ 层的输入误差，经过该层线性变换 $W^{l+1}$ 与本层自身量化误差 $\varepsilon^{l+1}$ 叠加后得到：

$$
\delta^{l+1} \approx W^{l+1}\,\delta^{l} + \varepsilon^{l+1}.
\tag{2-7}
$$

公式 (2-7) 表明：上层累积误差被本层权重矩阵的谱范数放大后再叠加本层量化误差，对一个 $L$ 层网络而言，末端累积误差可达单层误差的 $\mathcal{O}(L \cdot \rho^{L})$ 量级（$\rho$ 为各层权重谱半径几何均值）。在 4 比特以下场景下，单层量化误差本身已显著，累积效应使最终输出严重偏离全精度模型，这是极低比特 PTQ 出现"精度断崖"的核心机制之一。

为缓解层间误差累积，BRECQ 等先进 PTQ 方法将相邻数层组织为"块"（Block），以**块级整体输出**为重构目标（详见 2.5.3 节），在优化过程中显式吸纳层间传播效应。然而块级重构只能减缓而无法根除累积放大，在 W2A2 等极端配置下仍存在显著精度缺口。

### 2.4.3 校准数据的分布失配

PTQ 仅使用 100~1024 张校准样本完成参数初始化与重构，几乎必然导致校准集与完整训练集间的统计分布失配（distribution mismatch），其影响主要体现在两个层面：

**（1）步长估计偏差与"尺度雪崩"**：校准样本数量有限，激活分布的高阶矩（最大值、99.9% 分位数等）极易被少数离群样本主导，使初始化步长偏离全集最优值。一旦校准集中包含若干非典型大幅激活，MinMax 或 Percentile 初始化会将步长拉伸到远超正常信号的程度，正常样本被压缩至极少格点上，等效量化精度断崖式下降——这一现象在工程实践中常被称为**尺度雪崩**（scale avalanche）。

**（2）重构阶段的过拟合与泛化性退化**：重构以校准集上的 MSE 为优化目标。若校准集的特征分布无法充分覆盖测试集，重构结果将过拟合到校准子集的局部统计模式上，量化模型在校准集上 MSE 极小但在测试集上精度大幅下降。QDrop[3] 已证明，引入随机激活丢弃机制可将优化轨迹引向更平坦的极小值，从而部分缓解这一过拟合。

随机均匀采样校准集的上述局限正是本文第 4 章"基于混合采样的校准集构建算法"所直接针对的问题——通过信息熵驱动的主动采样策略，从数据源头提升校准集的代表性与鲁棒性，为 PTQ 的所有下游环节提供更高质量的数据基础。

### 2.4.4 全精度模型损失景观的几何约束

上述三类难点均聚焦于 PTQ 算法的"事后优化"过程本身，本文进一步发现了一个更深层的根本性瓶颈：**全精度模型损失景观的几何特性，对 PTQ 精度设置了无法通过任何事后优化方法突破的硬天花板**。

现有 PTQ 方法均隐含地把预训练全精度模型视为一个"完美教师"，量化的任务就是尽可能对齐教师输出。然而，对标准 SGD 训练所得模型的 Hessian 谱密度分析表明：这类模型通常收敛于一个**锐利**（sharp）的损失极小值——Hessian 迹较大，损失曲面在极小值附近呈陡峭形态。当 PTQ 把连续浮点权重映射到离散量化格点时，量化误差会将模型从极小值处推离，沿陡峭损失面迅速攀升，导致性能急剧下降。

形象地说，标准 SGD 模型居于"尖峰"之顶，量化误差只需轻微推动即可使其从峰顶跌落、精度断崖；而若模型本身位于"宽谷"之中，量化误差将被宽谷自然吸收，量化模型仍能保持高精度。这一发现意味着：

> **极低比特 PTQ 的精度瓶颈不仅是算法问题，更是基础模型的几何问题。无论事后重构算法如何精妙，只要全精度模型的极小值本身锐利，所有 PTQ 方法的精度都将受到严苛的几何上限约束。**

这一洞察正是本文第 3 章"量化前高效调优框架（ETBQ）"的研究出发点——在 PTQ 之前主动重塑全精度模型的损失景观，将其从"尖峰"整形为"宽谷"，从根本上抬高包括 QDrop 在内所有 PTQ 方法的精度上界。

---

## 2.5 训练后量化的基准方法

本节按方法演进的历史脉络介绍当前 PTQ 领域的四种代表性基准方法——朴素 PTQ、AdaRound、BRECQ 与 QDrop，系统梳理各自的核心思想、数学原理及与本文工作的关系，并明确本文所采用的基准选择。

### 2.5.1 朴素训练后量化（Naive PTQ）

朴素 PTQ 是最简单直接的量化方案，其核心思路为：利用校准集 $\mathcal{D}_c$ 初始化每层量化参数 $(s, z)$ 后，直接对全精度模型按公式 (2-2) 完成量化导出，**不进行任何参数优化**。完整流程见算法 2-1。

**算法 2-1 朴素训练后量化**
**Algorithm 2-1 Naive Post-Training Quantization**

```
输入：全精度模型 f(x; W)，校准集 D_c，目标量化比特数 q
输出：量化模型 f_q(x; W_q)
─────────────────────────────────────────────────────
1: 将 D_c 输入 f(x; W) 进行前向推理，
   收集各层权重和激活值的统计量 (x_max, x_min, ...)；
2: 按式 (2-5) 的 MinMax 或 MSE 准则计算各层
   初始量化参数 (s, z)；
3: 利用 (s, z) 按式 (2-2) 对各层权重 W 进行量化，
   得到量化权重 W_q；
4: 输出量化模型 f_q(x; W_q)。
```

朴素 PTQ 的优点是**速度极快**（数秒到数分钟）且**实现极简**，是 8 比特通用量化部署的事实标准。但在 4 比特以下场景下，由于量化参数仅由静态分布统计确定、无法感知量化误差对下游层输出的影响，其精度损失迅速变得不可接受——这正是 2.4.1～2.4.2 节所揭示难点的直接体现。

### 2.5.2 自适应舍入（AdaRound）

AdaRound[1] 是 PTQ 引入"重构优化"思想的开创性工作。其核心洞察是：权重量化中的舍入决策（向下 floor 还是向上 ceil）并不应简单地遵循"就近取整"，而应**以最小化对下游层输出的影响为目标，通过学习确定最优舍入方向**。

具体地，AdaRound 为每个待量化权重 $w_i$ 引入一个辅助变量 $h_i \in [0, 1]$ 表示舍入方向（趋近 0 为向下，趋近 1 为向上），并通过 Sigmoid 函数对其参数化以保证连续可微。第 $l$ 层的优化目标为最小化层级输出 MSE 并附加正则化项：

$$
\min_{h}\ \mathbb{E}_{x\in\mathcal{D}_c}\!\left[\big\| W^{l}\,x - \hat{W}^{l}(h)\,x \big\|_{F}^{2}\right] + \lambda\,f_{\text{reg}}(h),
\tag{2-8}
$$

其中 $\hat{W}^{l}(h)$ 为由 $h$ 控制的量化权重，$f_{\text{reg}}$ 是将 $h$ 渐进推向 $\{0,1\}$ 二值的正则项，$\lambda$ 为权重系数。

**AdaQuant[16] 进一步扩展了 AdaRound 的优化范式**：将量化参数 $(s, z)$ 乃至权重本身都视为连续可优化变量，借助 STE 在校准集上以梯度下降直接最小化层/块级输出 MSE。这一从"静态统计"到"动态学习"的连续优化范式，为后续的 BRECQ、QDrop 等所有基于梯度的重构方法奠定了方法论基础。

AdaRound（及其延伸 AdaQuant）的核心局限在于采用**逐层粒度**，未能显式建模 2.4.2 节所描述的层间误差累积传播效应，在 W2A4 以下精度仍然不足。

### 2.5.3 块级重构（BRECQ）

BRECQ[2] 将重构粒度由"逐层"扩展到"逐块"（Block-wise），以网络中天然的残差块（Residual Block）或 Transformer Block 为基本优化单元，**在块内联合优化所有层的量化参数**，从而显式吸纳块内层间误差的传播路径。其块级重构目标为：

$$
\min_{\{s^{l}, z^{l}\}_{l\in B}}\ \mathbb{E}_{x\in\mathcal{D}_c}\!\left[\big\| y_{B} - \hat{y}_{B} \big\|_{F}^{2}\right],
\tag{2-9}
$$

其中 $y_B$ 与 $\hat{y}_B$ 分别为块 $B$ 的全精度输出与量化输出。BRECQ 的关键理论贡献是引入 **Fisher 信息矩阵**作为 Hessian 的高效代理，用以加权各层量化误差对整体网络性能的贡献，并从理论上证明了块级重构在校准集到测试集的泛化能力上严格优于逐层重构。

BRECQ 的块级框架已成为后续众多 PTQ 方法（包括扩散模型量化 Q-Diffusion[15] 等复杂任务）的事实标准。然而其本质仍是 2.4.4 节所指的"事后补救"范式，无法突破全精度模型损失景观所设的几何上限。

### 2.5.4 随机丢弃重构（QDrop）

QDrop[3] 在 BRECQ 的块级重构框架之上，将**损失景观平坦性**与量化模型泛化性能首次明确关联。其核心观察是：直接在校准集上做块级重构容易过拟合——量化模型在校准集上的输出与全精度模型高度一致，但在测试集上存在明显泛化差距。

为此，QDrop 在重构的每次前向传播中以概率 $p$ 随机将某些层的量化激活替换为对应的全精度激活（即"丢弃"该层的量化效果），并理论证明该机制**等价于引导优化器在损失景观中向更平坦的极小值收敛**，使量化模型对量化噪声拥有更强的天然鲁棒性。完整流程见算法 2-2。

**算法 2-2 QDrop 量化重构优化**
**Algorithm 2-2 QDrop Quantization Reconstruction**

```
输入：全精度模型 f(x; W)，初始量化模型 f_q(x; W_q)，
      校准集 D_c，块迭代步数 T，激活丢弃概率 p
输出：优化后的量化模型 f_q*(x; W_q*)
─────────────────────────────────────────────────────
1: 利用 D_c 完成朴素 PTQ 初始化得到 (s, z)；
2: for 每个残差块 B in 网络 do:
3:     for t = 1 to T do:
4:         从 D_c 采样小批量 x_batch；
5:         前向时对 B 内每层激活量化操作以概率 p
           随机替换为全精度激活；
6:         按式 (2-9) 计算块级重构 MSE；
7:         经 STE 反传梯度，联合更新 (s^l, z^l) 及
           AdaRound 风格的舍入变量 h；
8:     end for
9: end for
10: 输出 f_q*(x; W_q*)。
```

本文以 **QDrop 作为 PTQ 后端基准方法**，主要原因有三：

1. **代表当前最优**：QDrop 在 W4A4、W2A4 等极低比特配置下代表了当前最先进的精度水平，是验证新方法竞争力的合适标尺；
2. **与本文研究视角契合**：QDrop 首次将损失景观平坦性引入 PTQ，这与本文第 3 章 ETBQ 所主张的"主动整形损失景观"思路具有理论上的天然对偶性，是验证 ETBQ 预调优有效性的理想测试床；
3. **模块化解耦良好**：QDrop 的块级框架可与 ETBQ 的预调优阶段、第 4 章的混合采样校准集无缝拼接，便于消融与公平对比。

### 2.5.5 基准方法的多维度对比

表 2-1 从重构粒度、优化目标、所需校准数据量、特有机制与极低比特精度五个维度对四种基准方法进行了系统对比。

**表 2-1 训练后量化基准方法综合对比**
**Tab. 2-1 Comprehensive comparison of representative PTQ baselines**

| 方法 | 重构粒度 | 优化目标 | 校准数据量 | 特有机制 | 极低比特精度 |
|:---|:---:|:---|:---:|:---|:---:|
| 朴素 PTQ | — | 分布统计 | ~100 张 | MinMax / MSE 初始化 | 差 |
| AdaRound[1] | 逐层 | 权重舍入 MSE | ~1000 张 | 可学习舍入方向 | 一般 |
| BRECQ[2] | 逐块 | 激活输出 MSE | ~1024 张 | Fisher 信息加权 | 良好 |
| QDrop[3] | 逐块 | 激活输出 MSE + 隐式平坦性 | ~1024 张 | 随机激活丢弃 | **优秀** |

从表 2-1 可以清晰看到 PTQ 方法的演化轨迹：从静态统计 → 可学习舍入 → 块级联合优化 → 引入平坦性视角，极低比特精度逐级提升。但所有上述方法都属于"事后补救"范式，最终精度均受限于 2.4.4 节所揭示的全精度模型几何上限。**本文第 3 章提出的 ETBQ 框架并非要替代上述任一方法，而是作为它们的上游增强模块——通过主动整形全精度模型的损失景观，从根本上拓宽包括 QDrop 在内所有 PTQ 方法的精度上界**。

---

## 2.6 本章小结

本章系统介绍了训练后量化（PTQ）的基本流程、特有难点与主流基准方法，为全文后续章节奠定理论基础。在内容组织上，本章刻意避免了与第 1 章 QAT 介绍的重复——量化的数学定义、对称/非对称量化、均匀/非均匀量化、舍入/截断误差以及 STE 的基本推导均不再重复展开，而是以"详见前章"的方式引用，仅就 PTQ 视角下的关键约定与对偶性结论（公式 2-3、2-4）做最小必要的回顾。

在 **PTQ 标准流程**方面，本章将 PTQ 抽象为"校准 → 重构 → 导出部署"三阶段标准方法论：校准阶段以小规模 $\mathcal{D}_c$ 完成量化参数初始化，重构阶段以层/块级输出 MSE 为目标进行有限梯度优化，导出部署阶段则将量化参数与权重融合为可硬件加速的真实低比特模型。明确了 PTQ 在数据隐私敏感、模型快速迭代、算力受限等工业场景下相较 QAT 的不可替代价值。

在 **PTQ 特有难点**方面，本章系统剖析了制约 PTQ 极低比特精度的四大核心瓶颈：（1）STE 在校准数据稀缺与有限迭代下的有偏估计放大；（2）量化误差沿前向传播的层间累积放大；（3）小规模校准集与全集间的分布失配引起的尺度雪崩与重构过拟合；（4）全精度模型损失景观的几何"锐利性"对 PTQ 精度设置的硬上限。前三者属于 PTQ 算法的内部局限，第四者则跳出了"事后补救"范式，直接构成本文第 3 章 ETBQ 框架的研究动机。

在 **PTQ 基准方法**方面，本章按演进顺序梳理了朴素 PTQ、AdaRound（及其延伸 AdaQuant）、BRECQ 和 QDrop 四种代表性方法的核心思想与数学原理，并以表 2-1 进行多维度系统对比，阐明了本文以 QDrop 作为 PTQ 后端基准的设计动机：QDrop 兼具"代表当前最优"、"与本文研究视角契合"、"与上游模块解耦良好"三方面优势，是公平评估本文所提 ETBQ 预调优框架与混合采样校准集贡献的理想测试床。

综上，本章共同构成了全文的方法论坐标系：第 3 章将以 2.4.4 节所揭示的"几何天花板"为出发点，提出 ETBQ 主动重塑全精度损失景观；第 4 章将以 2.4.3 节所揭示的"分布失配"为出发点，提出基于混合采样的校准集构建算法。两条研究主线最终都将以 2.5 节所介绍的 QDrop 为公共后端基准进行实验验证。

---

## 参考文献（本章）

[1] Nagel M, Amjad R A, van Baalen M, et al. Up or Down? Adaptive Rounding for Post-Training Quantization[C]//ICML. 2020: 7197-7206.

[2] Li Y, Gong R, Tan X, et al. BRECQ: Pushing the Limit of Post-Training Quantization by Block Reconstruction[C]//ICLR. 2021.

[3] Wei X, Gong R, Li Y, et al. QDrop: Randomly Dropping Quantization for Extremely Low-bit Post-Training Quantization[C]//ICLR. 2022.

[4] Esser S K, McKinstry J L, Bablani D, et al. Learned Step Size Quantization[C]//ICLR. 2020.

[5] Nagel M, Fournarakis M, Amjad R A, et al. Overcoming Oscillations in Quantization-Aware Training[C]//ICML. 2022.

[6] Gholami A, Kim S, Dong Z, et al. A Survey of Quantization Methods for Efficient Neural Network Inference[J]. Low-Power Computer Vision, 2022.

[7] Cai Y, et al. ZeroQ: A Novel Zero Shot Quantization Framework[C]//CVPR. 2020.

[8] Liu Z, et al. Nonuniform-to-Uniform Quantization: Towards Accurate Quantization via Generalized Straight-Through Estimation[C]//CVPR. 2022.

[9] Miyashita D, Lee E H, Murmann B. Convolutional Neural Networks Using Logarithmic Data Representation[J]. arXiv:1603.01025, 2016.

[10] Zhang D, et al. LQ-Nets: Learned Quantization for Highly Accurate and Compact Deep Neural Networks[C]//ECCV. 2018.

[11] Han S, et al. Deep Compression: Compressing Deep Neural Networks with Pruning, Trained Quantization and Huffman Coding[C]//ICLR. 2016.

[12] Bengio Y, Léonard N, Courville A. Estimating or Propagating Gradients Through Stochastic Neurons[J]. arXiv:1308.3432, 2013.

[13] Hinton G E. Neural Networks for Machine Learning: Lecture 9a[R]. Coursera, 2012.

[14] Nagel M, Fournarakis M, Amjad R A, et al. Overcoming Oscillations in Quantization-Aware Training[C]//ICML. 2022.

[15] Li Y, et al. Q-Diffusion: Quantizing Diffusion Models[C]//ICCV. 2023.

[16] Hubara I, Nahshan Y, Hanani Y, et al. Improving Post Training Neural Quantization: Layer-wise Calibration and Integer Programming[C]//ICML. 2021.
