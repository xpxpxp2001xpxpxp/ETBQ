# 第3章 面向低比特训练后量化的损失平坦化方法研究

## 3.1 引言

训练后量化（Post-Training Quantization, PTQ）技术自提出以来，经历了从朴素舍入到基于Hessian信息的精细化重构的演进历程。早期方法如最近邻舍入（Round-to-Nearest, RTN）直接将浮点权重映射到最近的量化格点，无需任何优化步骤。RTN虽然高效，但在低比特场景下精度损失严重，因为它忽略了量化决策对层级输出的影响。

AdaRound是PTQ领域的重要里程碑，其核心洞察是：对于逐层的重构误差最小化问题，并非所有权重都应简单地向下或向上舍入。通过引入二阶信息（Hessian矩阵），AdaRound为每个权重学习最优的舍入决策，在保持层级输出一致性的同时显著降低量化误差。BRECQ在AdaRound的基础上将优化粒度从逐层扩展到逐块（Block-wise），以Fisher信息矩阵作为Hessian的代理，在块内联合优化所有层的量化参数，有效缓解了层间误差的累积传播问题，其块级重构策略已成为后续PTQ方法的基础范式。QDrop进一步将损失景观平坦性与泛化性能联系起来，其核心思想是：量化模型的泛化能力与其损失景观的平坦程度正相关。通过在重构阶段以一定概率随机丢弃量化激活，QDrop引导优化器向更平坦的极小值收敛，从而提升量化模型在校准集之外的泛化能力。

尽管上述方法取得了显著进展，它们都共享一个根本性局限：所有现有PTQ方法均属于**事后补救**（post-hoc）范式，即以一个固定的、预训练好的全精度模型作为起点，在此基础上进行量化参数的优化。这一范式隐含地假设预训练的全精度模型本身即为一个理想的"教师模型"，量化的任务仅仅是尽可能对齐其输出。

本章的研究揭示了一个长期被忽视的关键问题：**通过标准SGD优化得到的全精度模型所收敛的局部极小值，本质上对量化误差并不友好**。具体而言，这类极小值通常具有较高的Hessian迹（即损失曲面在极小值附近锐利），对参数的微小扰动极为敏感。当PTQ将连续的浮点权重映射到离散的量化格点时，不可避免地引入量化误差，这些误差会将量化模型推离全精度模型所在的极小值，沿陡峭的损失曲面迅速上升，从而导致严重的精度损失。形象地说，如果全精度模型位于一个尖锐的"山峰之顶"，量化误差会将量化模型从峰顶推落、精度急剧下降；而如果全精度模型位于一个宽广的"平坦盆地"之中，量化误差的影响将被宽阔的盆地所吸收，量化模型仍能保持较高精度。

平坦的极小值意味着在参数空间中损失函数变化缓慢，模型对参数扰动具有天然的鲁棒性，因而拥有更强的泛化能力。随机权重平均（Stochastic Weight Averaging, SWA）通过对SGD训练轨迹上的多个权重点取平均，能够将模型导向损失曲面上更宽广的平坦区域，在多个基准任务上显示出优于常规SGD训练的泛化性能。此外，噪声注入作为一种隐式正则化手段，在各类鲁棒性研究中也有广泛应用：Dropout通过随机置零神经元激活、隐式地对网络施加噪声约束；对抗训练（Adversarial Training）通过在训练过程中注入对抗扰动，使模型对最坏情况下的输入变化保持鲁棒；标签平滑（Label Smoothing）则通过软化目标标签来隐式地引导优化器远离尖锐极小值。这些工作共同揭示了一个重要原则：**在训练阶段主动引入与目标鲁棒性一致的噪声，可以从根本上改善模型在对应扰动下的稳定性**。

现有PTQ方法的根本困境由此显现：无论校准算法多么精妙，只要全精度模型的极小值本身足够锐利，量化误差对性能的破坏就难以从根本上消除。这正是PTQ在极低比特下出现"精度断崖"的深层原因。本章发现，QDrop虽在PTQ阶段注入激活噪声，但其操作仍属于事后补救，且所注入的噪声并未对真实量化误差的统计分布进行精确建模；量化感知训练（QAT）方法（如NIPQ）虽在训练阶段注入伪量化噪声，但需要完整数据集进行重训练，不适用于数据稀缺的PTQ场景。

基于上述分析，本章提出了一种全新的研究范式：**与其在量化之后对量化误差进行事后补救，不如在量化之前主动地对全精度模型的损失曲面进行修缮，使其对量化误差具有强容忍性**。这一思路的核心在于：通过在预调优阶段注入经过精确统计建模的量化误差，将全精度模型从锐利的极小值主动迁移至足够宽广的平坦极小值，使得后续PTQ引入的量化误差能够被这个平坦的损失曲面自然吸收，而不会导致严重的精度损失。

**图3-1 ETBQ算法整体框架图**

本章的主要贡献如下：

1. **提出了全新的几何视角**：首次从损失景观几何特性的角度，系统阐明了标准SGD训练所产生的尖锐极小值是制约极低比特PTQ性能的根本性瓶颈，而非仅仅是量化误差本身。通过Hessian谱密度分析与量化误差注入实验，从理论与实验两个层面验证了这一论断，为PTQ研究提供了全新视角。

2. **提出了ETBQ框架**：提出量化前高效调优框架ETBQ（Efficient Tuning Before Quantization）。该框架包含两个相互解耦的量化噪声注入模块：权重量化噪声模块（Weight Quantization Noise, WQN）和激活量化噪声模块（Activation Quantization Noise, AQN）。WQN通过差分噪声注入机制确保权重侧优化的渐近无偏性，同时隐式地对Hessian迹进行正则化；AQN通过显著性感知随机掩码机制，对关键激活通道施加精确的量化误差，迫使网络学习对噪声不变的鲁棒表示。ETBQ采用解耦设计，有效避免了权重与激活量化误差量级悬殊导致的联合优化不稳定问题。

3. **取得了显著的实验效果**：在CIFAR-100、Tiny-ImageNet和Cityscapes数据集上，针对图像分类与语义分割任务进行了系统性评估。ETBQ在所有架构和量化配置下均取得领先结果：在CIFAR-100上，ResNet-50的W4A4量化精度超越基线0.52%，MobileNetV2的W4A4量化精度超越基线1.53%；在UNet语义分割W4A4配置下，ETBQ量化模型在Cityscapes上的mIoU（70.03%）甚至超越全精度SGD基线（68.52%）达1.51个百分点，充分彰显了损失景观预整形的独特价值。

---

## 3.2 相关工作

### 3.2.1 训练后量化方法

#### 3.2.1.1 最小化层级量化误差的目标

PTQ最朴素的思路是针对每一层，独立地最小化量化前后的权重差异。RTN对每个权重直接舍入到最近的量化格点，无需优化步骤。RTN虽然高效，但在低比特场景下精度损失严重，因为它忽略了量化决策对层输出的影响。

GPTQ是层级误差最小化范式在大语言模型（LLM）场景下的重要代表，其基于Optimal Brain Surgeon（OBS）框架，利用Hessian逆矩阵信息依次量化各权重，每次量化后对剩余权重进行补偿性更新，以消除量化累积误差。GPTQ以极小的性能损失实现了GPT系列模型的4比特量化，展现了Hessian信息在PTQ中的重要价值。

AdaRound将层级优化的目标从最小化权重距离转变为最小化层输出激活的均方误差（MSE）。其关键创新在于引入了可学习的舍入策略：为每个权重引入一个辅助变量（通过Sigmoid函数参数化），在优化过程中学习其应向下或向上舍入。通过引入正则化项平衡量化误差与舍入决策的一致性，AdaRound在少量数据下即可达到接近QAT的精度，成为现代PTQ方法的重要基准。

#### 3.2.1.2 块级激活量化误差的重构目标

逐层优化的局限在于它忽略了层间的误差传播：前一层的量化误差会被后续层放大，导致网络末端的误差远大于各层局部误差之和。块级优化（Block-wise Optimization）通过将相邻若干层组织为一个块、以块的整体输出作为优化目标，有效捕获了层间误差传播效应。

BRECQ是块级优化范式的奠基性工作。它以Fisher信息矩阵作为Hessian的高效代理，在每个残差块内联合优化所有层的量化参数，并通过理论分析证明块级优化在误差泛化能力上优于逐层优化。BRECQ的块级策略已成为后续众多PTQ方法的标准框架，并被成功扩展到扩散模型量化（Q-Diffusion）等复杂任务中。

QDrop在BRECQ框架的基础上进一步引入了损失平坦性的视角。其核心观察是：直接在校准集上进行激活重构容易导致过拟合——量化模型在校准数据上的输出与全精度模型高度一致，但在测试数据上却存在明显泛化差距。为此，QDrop在重构阶段以一定概率将网络中各层的激活随机替换为全精度激活，并证明这等价于引导优化器在损失景观中向更平坦的极小值移动。QDrop的核心贡献在于将量化精度问题与损失景观几何特性明确联系起来，这一洞察也是本章研究的重要出发点。

MRECG（Mixed-precision Reconstruction with Error-guided Granularity）针对不同层量化敏感性差异显著的问题，提出自适应的重构粒度选择策略：对量化误差较大的敏感层采用细粒度（逐层）重构，对不敏感层采用粗粒度（块级）重构，在计算效率与精度之间取得更优平衡。

#### 3.2.1.3 面向平坦性的量化方法

近年来，研究者开始意识到PTQ的事后补救本质存在固有局限，并尝试从不同角度突破这一瓶颈。FlatQuant针对大语言模型权重分布存在异常值（outlier）的问题，通过学习可微的仿射变换对权重和激活进行重参数化，使其分布更加均匀，从而改善量化映射的精度。FlatQuant与本章工作存在重要区别：FlatQuant通过改变参数空间的几何形状（等价变换权重矩阵）来间接影响量化友好性，而本章的ETBQ则直接修改损失景观的几何特性；此外，FlatQuant是在全精度模型固定后进行的重参数化，仍属于某种意义上的"后处理"，而ETBQ则是在量化之前对全精度模型本身进行主动的景观整形。

Quantization without Tears提出了平滑渐进量化近似与自适应梯度缩放技术，以缓解极低比特下非光滑损失景观带来的优化困难。但该方法嵌入在QAT框架中，需要完整数据集重训练，不适用于数据稀缺的PTQ场景。

本章提出的ETBQ与上述方法的本质区别在于：**ETBQ在PTQ流程之前进行干预**，通过统计建模真实量化误差并将其注入训练过程，主动塑造全精度模型的损失景观几何特性，为后续任意PTQ算法提供一个对量化误差鲁棒的起点。

### 3.2.2 损失景观平坦性

#### 3.2.2.1 平坦极小值的理论基础

损失景观（Loss Landscape）的几何特性与深度网络的泛化性能之间存在密切联系，这一联系在深度学习理论界已得到广泛研究。直觉上，一个宽广平坦的极小值意味着在该极小值附近，损失函数对参数的变化不敏感：即使参数受到一定幅度的扰动，损失值也不会显著增加，模型的预测行为也基本保持稳定。相反，一个尖锐的极小值对参数扰动极为敏感：微小的参数变化即可导致损失值急剧上升。

Hessian矩阵（损失函数对参数的二阶导数矩阵）是描述损失景观局部几何特性的数学工具。Hessian矩阵的特征值反映了损失函数在不同参数方向上的曲率：较大的特征值对应陡峭方向，较小的特征值对应平坦方向。常用的平坦性度量指标包括Hessian迹（所有特征值之和）、Hessian谱密度（特征值的分布）以及最大特征值（Hessian范数）。

从优化理论的角度看，随机梯度下降（SGD）由于其固有的梯度噪声，在某种程度上具有趋向平坦极小值的隐式偏好。这是因为在尖锐极小值附近，较大的Hessian特征值会放大梯度噪声，导致优化轨迹不稳定；而在平坦区域，梯度噪声对参数更新的干扰相对较小，优化更易收敛。然而，标准SGD对平坦极小值的偏好仅是一种软约束，在实际训练中并不能保证收敛到对量化友好的、足够宽广的极小值。

#### 3.2.2.2 平坦极小值的寻找方法

随机权重平均（SWA）是目前最广泛使用的平坦极小值寻找方法之一。SWA的原理简洁而有效：在SGD训练的后期，不再仅保留最终的权重快照，而是对训练轨迹上的多个等间隔检查点权重取算术平均。几何直觉上，SGD轨迹倾向于在某个宽广盆地的边缘绕行，而对轨迹取平均可将参数拉向盆地中心，使其处于一个比任何单个检查点都更平坦的位置。SWA已在图像分类、语义分割等多个基准任务上被证明相比常规SGD具有更强的泛化性能。

锐度感知最小化（Sharpness-Aware Minimization, SAM）是近年来提出的另一种平坦极小值优化方法。SAM的核心思想是将优化目标从最小化当前参数处的损失，改变为最小化参数邻域内的最大损失（即最坏情况下的损失），从而显式地驱使优化器趋向"最坏扰动下损失仍然较低"的平坦区域。SAM通过两步优化过程实现这一目标：首先沿梯度上升方向搜索邻域内的损失峰值，然后沿该峰值处的梯度下降方向更新参数。SAM在多个基准任务上取得了优异的泛化性能，但其每次迭代需要计算两次梯度，训练成本是常规SGD的两倍。

SWAG（SWA-Gaussian）将SWA与贝叶斯深度学习相结合，用SGD轨迹的统计特性近似后验分布的协方差，从而构建一个以SWA均值为中心的高斯参数后验。SWAG不仅提供了平坦极小值的点估计，还给出了参数不确定性的度量，在不确定性量化任务中具有重要价值。

需要指出的是，上述方法在设计上均与量化无关：它们寻找的是泛化意义上的平坦极小值，而非针对特定量化误差分布对齐的平坦极小值。本章的ETBQ则通过统计建模真实量化误差并将其注入训练过程，确保所寻找的平坦极小值与目标比特宽度的量化扰动网格精确对齐，从而实现"量化精度对齐的平坦性"。

### 3.2.3 噪声注入与模型鲁棒性

#### 3.2.3.1 隐式正则化

在深度学习训练中，噪声注入是一类重要的隐式正则化技术，其核心思想是通过在训练过程中向网络输入、中间激活或参数中引入受控的随机扰动，使网络学习到对该类扰动具有鲁棒性的特征表示。

Dropout是最经典的激活层噪声注入方法，通过随机将一定比例的神经元激活置为零，隐式地实现了模型集成（Ensemble）的效果，有效防止过拟合并提升泛化能力。Dropout可以从噪声注入的角度理解：被置零的神经元相当于在激活中注入了乘性Bernoulli噪声。

标签平滑（Label Smoothing）通过将硬标签（one-hot 向量）替换为软标签（在目标类别分配 $1-\varepsilon$ 的概率，在其余类别均匀分配 $\varepsilon$ 的概率），等价地在目标概率分布中注入了均匀噪声。标签平滑已被证明能够隐式地防止模型过于自信，并引导优化器远离尖锐极小值。

在参数层面，Weight Perturbation方法在梯度计算时对参数施加小幅随机扰动，从而在每次迭代中隐式地评估损失景观的局部平坦性，并驱使参数向更平坦的区域移动。DropConnect则随机将权重而非激活置零，实现了与Dropout类似但作用层次不同的正则化效果。

#### 3.2.3.2 面向量化鲁棒性的噪声注入

将噪声注入技术专门用于提升量化鲁棒性，是近年来量化领域的一个重要研究方向。这类方法的核心思想是：通过在训练阶段注入模拟量化误差的噪声，使模型在参数空间中预先适应量化带来的离散扰动。

QAT中广泛使用的直通估计器（Straight-Through Estimator, STE）可以视为一种特殊的噪声注入：在前向传播中使用量化后的参数，在反向传播中直接将梯度"直通"量化操作（即忽略量化函数的非连续性），等价地在梯度信号中引入了噪声。然而，STE本身存在梯度偏差问题，可能导致训练不稳定。

NIPQ（Noise Injection-based Pseudo Quantization）提出了一种更为精细的量化噪声注入策略：将量化操作替换为连续的高斯噪声注入，其中噪声的方差与量化步长相关联，从而在保持梯度可微的同时近似真实量化误差的影响。NIPQ在QAT框架中同时处理权重和激活的量化噪声，取得了优于传统STE的训练稳定性和量化精度。然而，NIPQ仍需要完整数据集进行重训练，属于QAT范式。

FQAT（Flatness-aware Quantization Aware Training）将损失景观平坦性正则化引入QAT框架，通过逐层梯度冻结策略平衡任务损失与曲率目标，在训练阶段同时优化精度与平坦性。但FQAT同样需要全数据集重训练，不适用于PTQ场景。

本章的ETBQ与上述方法的核心区别在于**解耦设计与适用场景**：ETBQ将"景观预整形阶段"与"量化优化阶段"完全解耦，仅需小规模校准集即可完成预调优，之后可与任意PTQ算法无缝衔接，兼具QAT的高精度优势与PTQ的数据效率优势。

---

## 3.3 方法

本节详细阐述量化前高效调优框架（ETBQ）的完整方法论。ETBQ的核心目标是：在对全精度（Full-Precision, FP）模型执行任何PTQ算法之前，通过注入经过统计建模的量化误差，主动将FP模型的损失极小值从尖锐重塑为平坦，使其天然能够吸收低比特量化引入的离散扰动。框架由三个协同工作的模块构成：权重量化噪声注入（WQN）、激活量化噪声注入（AQN），以及基于随机权重平均（SWA）的平坦性巩固。

### 3.3.1 符号定义与问题建模

**(1) 基本符号定义**

本章采用如下符号体系：矩阵（或张量）用大写符号 $X$ 表示，向量用小写 $x$ 表示，$f(x;w)$ 表示权重参数为 $w$、输入为 $x$ 的全精度模型，$f(x;w,s,z)$ 则表示包含权重参数 $w$、量化参数 $s$ 与 $z$ 以及输入 $x$ 的量化模型。假设样本来源于训练集 $\mathcal{D}_t$。

**(2) 量化相关概念定义**

步长 $s$ 与零点 $z$ 是浮点表示与定点表示之间的桥梁。给定输入张量 $x$（此处可为特征 $x$ 或权重 $w$），量化操作定义如下：

$$
\begin{aligned}
x_{\text{int}} &= \text{clip}\left(\left\lfloor \frac{x}{s} \right\rceil + z,\ 0,\ 2^{q}-1\right), \\
\hat{x} &= (x_{\text{int}} - z) \cdot s,
\end{aligned}
\tag{3-1}
$$

其中 $\lfloor \cdot \rceil$ 为四舍五入函数，$q$ 为预设量化位宽，$s$ 为量化步长，$z$ 为零点参数。$s$ 与 $z$ 均通过从训练集 $\mathcal{D}_t$ 中选取的校准集 $\mathcal{D}_c$（$\mathcal{D}_c \subset \mathcal{D}_t$）进行初始化：

$$
s = \frac{x_{\max} - x_{\min}}{2^{q}-1},
\tag{3-2}
$$

$$
z = \left\lfloor q_{\max} - \frac{x_{\max}}{s} \right\rceil,
\tag{3-3}
$$

其中 $q_{\max}$ 为量化整数的上界。

量化误差可分解为权重量化误差与激活量化误差两项，分别记为 $\delta w = \hat{w} - w$ 和 $\delta x = \hat{x} - x$。对于线性投影层，量化前后输出的均方误差展开如下：

$$
\begin{aligned}
\mathcal{L}_{\text{MSE}} &= \mathbb{E}_{x}\!\left[\|wx - \hat{w}\hat{x}\|_2^2\right] \\
&= \mathbb{E}_{x}\!\left[\|wx - (w+\delta w)(x+\delta x)\|_2^2\right] \\
&= \mathbb{E}_{x}\!\left[\|w\,\delta x + \delta w \cdot x + \delta w\,\delta x\|_2^2\right].
\end{aligned}
\tag{3-4}
$$

式 (3-4) 将重构误差明确分解为**激活量化误差项**（$w\,\delta x$）、**权重量化误差项**（$\delta w \cdot x$）以及**高阶交叉项**（$\delta w\,\delta x$）。如图3-2所示，使用ResNet-18模型在CIFAR-100公开图像分类数据集上计算量化误差，观察第四个残差模块 `block.3.conv2` 中第20个通道的误差分布可以发现：激活量化误差项 $w\,\delta x$ 的量级远大于权重量化误差项 $\delta w \cdot x$（在极低比特下差距可达两个数量级以上）。若将两者混合优化，量级失衡会导致严重的梯度方向冲突和训练不稳定。因此，本章的ETBQ框架采用**解耦设计**，以WQN与AQN分别独立处理上述两类误差源。

**图3-2 ResNet-18 在 CIFAR-100 上 `block.3.conv2` 第20个通道的权重量化误差项 $\delta w \cdot x$ 与激活量化误差项 $w\,\delta x$ 的分布对比**

### 3.3.2 权重量化噪声注入

#### 3.3.2.1 权重量化误差的统计建模

权重量化噪声（WQN）注入模块的核心思想是：摒弃确定性的舍入截断，将量化误差解耦为一种高频的差分物理微扰，以此在全精度训练中隐式地寻找平坦极小值。为此，本章首先对权重量化误差进行精确的统计建模——在微调阶段每个 Epoch 初期，利用校准集 $\mathcal{D}_c$ 对全精度模型执行一次模拟量化，计算每层、每个输出通道的量化误差张量 $E_w = \hat{w} - w$。

**图3-3 ResNet-18 在 CIFAR-100 上的权重分布（左）与量化误差分布（右）**

实验观察表明（图3-3 分别选取 ResNet-18 中第四残差模块 `block.3.conv1` 与第五残差模块 `block.4.conv1` 的第10通道展示其在 CIFAR-100 上的权重分布与量化误差），在通道级量化粒度下，各通道的量化误差近似服从高斯分布。对于卷积层权重张量 $w \in \mathbb{R}^{C_{\text{out}} \times C_{\text{in}} \times K_H \times K_W}$，第 $i$ 个输出通道的误差子张量 $E_{w,i}$ 包含 $N_i = C_{\text{in}} \cdot K_H \cdot K_W$ 个元素，该通道误差的均值 $\mu_{w,i}$ 与方差 $\sigma_{w,i}^{2}$ 计算如下：

$$
\mu_{w,i} = \frac{1}{N_i} \sum_{j,h,k} E_{w,i,j,h,k}, \qquad
\sigma_{w,i}^{2} = \frac{1}{N_i} \sum_{j,h,k} \left(E_{w,i,j,h,k} - \mu_{w,i}\right)^{2}.
\tag{3-5}
$$

由此得到每层的均值向量 $\boldsymbol{\mu}_w \in \mathbb{R}^{C_{\text{out}}}$ 与方差向量 $\boldsymbol{\sigma}_w^{2} \in \mathbb{R}^{C_{\text{out}}}$，构成量化误差分布 $\mathcal{N}(\boldsymbol{\mu}_w, \boldsymbol{\Sigma}_w)$，其中 $\boldsymbol{\Sigma}_w = \text{diag}(\boldsymbol{\sigma}_w^{2})$。

#### 3.3.2.2 差分噪声注入机制

获得误差分布 $\mathcal{N}(\boldsymbol{\mu}_w, \boldsymbol{\Sigma}_w)$ 后，一种最直接的方案是在每个 SGD 步骤 $t$ 向权重注入独立同分布的采样噪声 $\delta_t \sim \mathcal{N}(\boldsymbol{\mu}_w, \boldsymbol{\Sigma}_w)$。然而，这一朴素方法存在根本性缺陷：由于所建模的量化误差均值 $\boldsymbol{\mu}_w \neq \mathbf{0}$，每步注入的噪声将引入一个恒定的期望偏移 $\mathbb{E}[\delta_t] = \boldsymbol{\mu}_w$，导致优化器实际上在追踪一个被系统性偏移的目标，造成梯度偏置并收敛到次优解。

为消除这一偏置，ETBQ 引入**时序反相关的差分噪声注入机制**。在每个训练步骤 $t$，利用当前步采样 $\delta_t$ 与上一步采样 $\delta_{t-1}$ 构造扰动权重：

$$
w_{t}^{\prime} = \tilde{w}_{t} + \lambda_e \cdot (\delta_t - \delta_{t-1}),
\tag{3-6}
$$

其中 $\tilde{w}_t$ 为当前物理权重，$\lambda_e \in [0,1]$ 为依训练轮次线性递增的噪声强度退火系数，$\delta_t, \delta_{t-1}$ 为相邻两步从 $\mathcal{N}(\boldsymbol{\mu}_w, \boldsymbol{\Sigma}_w)$ 独立采样的噪声向量。差分项 $P_t = \lambda_e \cdot (\delta_t - \delta_{t-1})$ 的期望为：

$$
\mathbb{E}[P_t] = \lambda_e \cdot \boldsymbol{\mu}_w - \lambda_e \cdot \boldsymbol{\mu}_w = \mathbf{0}.
\tag{3-7}
$$

也就是说，差分注入机制天然消除了均值偏移，并且在整个训练期间 $T$ 步求和后，差分项形成望远镜求和（Telescoping Sum），即 $\sum_{t=1}^{T} P_t = \lambda_e \cdot (\delta_T - \delta_0)$，其期望累积为有界常数 $\mathbf{0}$，随 $T \to \infty$ 渐近无偏（详见 3.4 节）。

为深入理解差分噪声注入的优化本质，引入潜在干净权重的等价表示。定义潜在权重 $w_t \triangleq \tilde{w}_t - \lambda_e \cdot \delta_t$，则式 (3-6) 等价于在扰动点 $w_t + \lambda_e \cdot \delta_{t+1}$ 处计算梯度后更新潜在权重：

$$
w_{t+1} = w_t - \eta \cdot \nabla \mathcal{L}\!\left(w_t + \lambda_e \cdot \delta_{t+1}\right).
\tag{3-8}
$$

公式 (3-8) 揭示了 WQN 真正优化的目标函数（即期望扰动损失）为：

$$
\tilde{\mathcal{L}}(w) = \mathbb{E}_{\delta}\!\left[\mathcal{L}(w + \lambda_e \cdot \delta)\right].
\tag{3-9}
$$

将噪声分解为 $\delta = \boldsymbol{\mu}_w + \varepsilon$，其中 $\varepsilon \sim \mathcal{N}(\mathbf{0}, \boldsymbol{\Sigma}_w)$，对期望扰动损失在量化中心点 $w + \lambda_e \cdot \boldsymbol{\mu}_w$ 处进行二阶泰勒展开，得到等效目标函数：

$$
\tilde{\mathcal{L}}(w) \approx \mathcal{L}(w + \lambda_e \cdot \boldsymbol{\mu}_w) + \frac{\lambda_e^{2}}{2}\,\text{Tr}\!\left(H_{w,\mu} \cdot \boldsymbol{\Sigma}_w\right),
\tag{3-10}
$$

其中 $H_{w,\mu} = \nabla^{2} \mathcal{L}(w + \lambda_e \cdot \boldsymbol{\mu}_w)$ 为在量化中心处计算的 Hessian 矩阵。从公式 (3-10) 可以看出，WQN 通过 $\mathcal{L}(w + \lambda_e \cdot \boldsymbol{\mu}_w)$ 与 $\frac{\lambda_e^{2}}{2}\,\text{Tr}(H_{w,\mu} \cdot \boldsymbol{\Sigma}_w)$ 这一对双重结构隐式地优化目标：

- 前者将优化目标从全精度权重 $w$ 转移到量化后的期望部署状态 $w + \lambda_e \cdot \boldsymbol{\mu}_w$，使模型直接在量化中心处最小化任务损失，而非在全精度点优化；
- 后者以量化误差方差 $\boldsymbol{\Sigma}_w$ 为权重，对量化中心处的 Hessian 迹施加隐式正则化惩罚，驱动优化器降低损失曲面在"量化误差方差最大方向"上的曲率，从而将极小值展平至与目标量化步长网格精确对齐的宽广形态。

这一理论分析表明，**WQN 并非盲目地平坦化损失景观，而是有方向性地将曲率正则化聚焦于量化误差实际扰动最大的参数方向上，实现了"量化精度对齐的平坦性"**。

### 3.3.3 激活量化噪声注入

激活量化误差的量级通常远大于权重量化误差，若将高方差的激活噪声直接注入所有空间位置，将造成特征图的严重破坏，导致梯度爆炸或训练崩溃。为此，AQN 提出**显著性感知随机掩码**（Salience-Aware Stochastic Mask）机制，将噪声注入精准聚焦于对量化最脆弱的关键激活通道。

在微调阶段每个 Epoch 初期，利用校准集 $\mathcal{D}_c$ 进行一次前向传播。与权重逐通道（per-channel）的统计不同，考虑到激活通道间的动态相关性以及计算效率，本章对激活采用**逐张量**（per-tensor）的全局统计模型。对于某一特定网络层的全精度激活张量 $A$ 及其量化后的张量 $A_q$，定义元素级激活误差张量为 $E_a = A_q - A$。

假设张量 $A$ 包含 $N$ 个元素，其全局均值 $\mu_a$ 与方差 $\sigma_a^{2}$ 作为高斯分布的先验参数：

$$
\mu_a = \frac{1}{N} \sum_{i=1}^{N} E_{a,i}, \qquad
\sigma_a^{2} = \frac{1}{N} \sum_{i=1}^{N} \left(E_{a,i} - \mu_a\right)^{2}.
\tag{3-11}
$$

为保证统计量在批次（mini-batch）采样间的平滑性与时间维度的稳定性，采用指数移动平均（EMA）进行动态更新：

$$
\mu_a^{(t)} = \beta \cdot \mu_a^{(t-1)} + (1-\beta) \cdot \mu_a, \qquad
\left[\sigma_a^{(t)}\right]^{2} = \beta \cdot \left[\sigma_a^{(t-1)}\right]^{2} + (1-\beta) \cdot \sigma_a^{2},
\tag{3-12}
$$

其中 $\beta$ 为动量系数。该平滑后的全局统计量构成了基础的激活噪声采样分布 $\mathcal{N}(\mu_a, \sigma_a^{2})$。

确立了噪声源 $\delta_a \sim \mathcal{N}(\mu_a, \sigma_a^{2})$ 后，核心挑战在于如何注入该噪声。为精准模拟离群点被量化截断的风险，AQN 将噪声注入的概率与激活值的绝对幅度（magnitude）强绑定。

设 $A$ 为当前批次展平后的激活特征向量，其总元素数为 $N$。首先，利用带温度系数 $T$ 的 Softmax 函数计算每个元素 $A_i$ 的相对显著性得分 $S_i$：

$$
S_i = \frac{\exp(|A_i|/T)}{\sum_{j=1}^{N} \exp(|A_j|/T)}.
\tag{3-13}
$$

公式 (3-13) 中的温度超参数 $T$ 起到了关键的调控作用：它在"极端硬攻击"与"平稳训练"之间达成了良好的平衡。若 $T \to 0$，分布将退化为硬性的 Top-K 选择（Hard Top-K），过度集中于极少数离群点会导致训练极不稳定；若 $T \to \infty$，则退化为均匀分布，丧失了对离群点的靶向免疫能力。

由于 Softmax 归一化后的单个得分 $S_i$ 极小，直接作为采样概率会导致注入的噪声密度趋近于零。为达到预设的目标噪声密度 $\rho$（本章默认设为 $\rho = 0.5$），将显著性得分映射为最终的注入概率 $p_i$：

$$
p_i = \min\!\left(\rho \cdot N \cdot S_i,\ 1\right).
\tag{3-14}
$$

基于概率 $p_i$，通过 Bernoulli 采样生成二值化的布尔掩码张量 $M_{\text{mask}}$：

$$
M_{\text{mask}} \sim \text{Bernoulli}(p).
\tag{3-15}
$$

最终，经过门控筛选的扰动激活特征 $A^{\prime}$ 由下式计算：

$$
A^{\prime} = A + M_{\text{mask}} \odot (\gamma \cdot \delta_a),
\tag{3-16}
$$

其中 $\odot$ 表示 Hadamard 乘积，$\gamma$ 为由高斯升温曲线控制的动态强度因子。

通过这种概率性的软 Top-K（Soft Top-K）门控机制，AQN 迫使网络中的关键决策路径学会抵抗量化截断引起的剧烈数值偏移。优化器在处理这种具有显著性偏好的随机掩码时，必须自发地降低对少数长尾离群点的过度依赖，从而展平了激活变差方向上的损失曲面，赋予了全精度底座极强的低比特量化韧性。

### 3.3.4 基于 SWA 的平坦性巩固

前文所述的 WQN 与 AQN 模块通过高斯指数升温策略，成功驱动模型跨越了尖锐的势垒。然而，当训练进入中后期（即 $\beta > E_{\text{warm-up}}$），高斯调度器会持续维持在峰值强度 $\gamma_{\max}$，进行双轨噪声的满载注入。从随机优化动力学的视角来看，此时的 SGD 优化器无法收敛到某一个绝对的静态点，而是会在平坦盆地（Flat Minima）的底部陷入持续的随机游走（Random Walk），形成一个围绕最优解的平稳分布（Stationary Distribution）。

在这种剧烈的动态平衡下，如果直接提取某一离散训练轮次（Epoch）的模型权重作为最终输出，极易引入偶然性的预测偏差。由于单次采样的权重可能恰好游走至平坦盆地的边缘，一旦在后续 PTQ 阶段再叠加确定性的量化截断位移，模型极易直接跌出具有高泛化能力的优良解空间。

为将动态的优化轨迹转化为静态的鲁棒底座，本章在微调的最后阶段（即训练轮次 $\beta \geq E_{\text{SWA}}$ 时）引入了 SWA 机制作为底层参数的定型策略。在 SWA 阶段，优化器不再执行学习率衰减，而是采用恒定或循环学习率（SWALR），以维持模型在平坦区域内的遍历探索能力。设 $n_{\text{SWA}}$ 为进入 SWA 阶段后的累积轮次，算法在每一个 Epoch 结束时，利用滑动算术平均对时序上的模型参数进行积分整合：

$$
W_{\text{SWA}} \leftarrow \frac{n_{\text{SWA}} \cdot W_{\text{SWA}} + W_{\text{current}}}{n_{\text{SWA}} + 1}.
\tag{3-17}
$$

其中 $W_{\text{current}}$ 为该轮次的权重参数。通过对时序上的模型参数进行集成，$W_{\text{SWA}}$ 能够精准地锚定在具有最高量化容差的平坦极小值的几何质心。

完成 SWA 参数聚合后，$W_{\text{SWA}}$ 是多个历史状态的均值，但网络中批量归一化（Batch Normalization, BN）层所保留的均值（Mean）与方差（Variance）依然停留在微调结束时的最终单点状态，这两者之间的统计分布错位会导致推理精度严重崩塌。因此，在输出最终模型前，本流程必须冻结网络参数，并将训练集数据（无须标签）重新输入聚合后的模型中进行若干次前向传播，以重新校准所有 BN 层的统计量：

$$
\hat{\mu}_{\text{BN}} = \mathbb{E}[x_{\text{SWA}}], \qquad
\hat{\sigma}_{\text{BN}}^{2} = \text{Var}[x_{\text{SWA}}].
\tag{3-18}
$$

经过 BN 层统计量的精确对齐后，整个 ETBQ 算法即输出最终的量化友好型高鲁棒模型。

### 3.3.5 训练流程

在深入探讨了权重与激活的解耦误差模拟机制（WQN 与 AQN）之后，本节以分类任务为例，系统性地推导 ETBQ 算法的整体优化目标与训练流程。

在分类任务中，设给定输入样本为 $x \in \mathbb{R}^{d}$，其对应的真实类别标签为 $y \in \{1, 2, \dots, K\}$，其中 $K$ 为总类别数。在 ETBQ 的微调前向传播中，网络权重 $W$ 与中间层激活值 $A$ 不再是静态张量，而是均被注入了与目标量化位宽（如 W4A4）严格挂钩的动态模拟量化噪声。

令扰动微调后的网络输出对数概率（Logits）为 $\tilde{z} \in \mathbb{R}^{K}$，其前向计算可表示为：

$$
\tilde{z} = f\!\left(x;\ W + \Delta W_{\text{diff}},\ A + \Delta A_{\text{gate}}\right),
\tag{3-19}
$$

其中 $\Delta W_{\text{diff}}$ 为 3.3.2 节定义的权重无偏差分噪声，$\Delta A_{\text{gate}}$ 为 3.3.3 节定义的基于显著性门控的激活噪声。

为避免在微调初期引入过大的非凸扰动导致梯度爆炸，本章不采用恒定的噪声强度，而是引入**高斯指数升温策略**（Gaussian Exponential Ramp-up）来动态调度 WQN 的扰动强度，定义为：

$$
\gamma = \gamma_{\max} \cdot \exp\!\left(-5 \cdot \left[1 - \left(\frac{\beta}{E_{\text{warm-up}}}\right)^{2}\right]\right),
\tag{3-20}
$$

其中 $\gamma_{\max}$ 表示 WQN 噪声注入的最大强度，$E_{\text{warm-up}}$ 表示预定义的预热阶段（warm-up）训练轮数，$\beta$ 为当前微调的轮次（Epoch），定义为：

$$
\beta = \text{clip}(t,\ 0,\ E_{\text{warm-up}}),
\tag{3-21}
$$

其中 $t$ 表示当前训练轮数索引。

**图3-4 不同 $E_{\text{warm-up}}$ 与 $\gamma_{\max}$ 设置下 WQN 强度 $\gamma$ 随 Epoch 的变化曲线**

参数 $\gamma$ 的值在训练初期（$\beta \to 0$）显著影响模型的初始状态。因此，初始阶段 $\gamma$ 应取较小值；随着训练进行，$\gamma$ 逐渐增加，并在训练后期趋于稳定。这一过程使得模型权重能够以最小的精度代价，平滑地向具有高量化容差的平坦盆地迁移。关于 $\gamma$ 强度的详细讨论见 3.5.4.1 节。

在极低比特量化（如 2 比特）场景下，传统的交叉熵（Cross-Entropy, CE）损失为了使正确类别的概率趋近于 1，会驱使 Logits 的绝对值无限增长。这不仅会导致损失曲面变得极其陡峭，还会使激活值的动态范围（Dynamic Range）急剧膨胀，进而引发严重的激活截断误差（Clipping Error）。

为限制激活尺度的无限增长并软化决策边界，本章引入**标签平滑机制**。令 $q$ 为真实标签 $y$ 的 one-hot 编码向量，平滑系数为 $\alpha \in (0,1)$（本章实验中默认取 $\alpha = 0.1$）。平滑后的软标签分布 $q^{ls}$ 定义为：

$$
q_{i}^{ls} =
\begin{cases}
1 - \alpha + \dfrac{\alpha}{K}, & i = y, \\[6pt]
\dfrac{\alpha}{K}, & i \neq y.
\end{cases}
\tag{3-22}
$$

结合量化噪声微调后的 Logits 输出 $\tilde{z}$ 与平滑后的目标分布 $q^{ls}$，ETBQ 算法在分类任务下的单步优化损失函数 $\mathcal{L}_{\text{ETBQ}}$ 最终定义为标签平滑交叉熵损失（Label-Smoothing Cross-Entropy, LSCE）：

$$
\mathcal{L}_{\text{ETBQ}}(x;\ W, A) = -\sum_{i=1}^{K} q_{i}^{ls} \cdot \log\!\left(\frac{e^{\tilde{z}_i}}{\sum_{j=1}^{K} e^{\tilde{z}_j}}\right).
\tag{3-23}
$$

由公式 (3-23) 可知，在反向传播 $\nabla_{W} \mathcal{L}_{\text{ETBQ}}$ 的过程中，SGD 优化器不仅要拉近预测分布与真实分布的 KL 散度，更要在梯度流中处理由 $\Delta W_{\text{diff}}$ 与 $\Delta A_{\text{gate}}$ 带来的剧烈非凸扰动。为使该损失函数收敛，优化器会引导权重 $W$ 逃离曲率极大的尖锐峡谷，迁移至"即使发生随机位移也不会导致 $\mathcal{L}_{\text{ETBQ}}$ 剧烈突变"的平坦盆地。

在高斯升温曲线达到峰值并维持一段时间后，由于持续的高强度噪声注入，单次 Epoch 的权重状态会在盆地底部发生随机游走。为此，当微调总轮次达到设定阈值 $\beta \geq E_{\text{SWA}}$ 后，算法启用 SWA 机制（详见 3.3.4 节），在每个 Epoch 结束时按式 (3-17) 对权重进行滑动算术平均，并最终按式 (3-18) 重新校准所有 BN 层的统计量。

**算法 3-1 ETBQ 算法流程**

```
Algorithm 3-1: ETBQ algorithm flow
---------------------------------------------------------------
Input : 全精度模型 f(x; W);  校准集 D_c;  训练集 D_t;
        目标比特宽度 (W_bit, A_bit);  总训练轮数 E_total;
        Warm-up 轮数 E_warm-up;  SWA 启动轮数 E_SWA;
        最大噪声强度 γ_max;  噪声密度 ρ;  动量系数 β;  温度 T;
        标签平滑系数 α;  学习率 η.
Output: 量化友好型全精度模型 W_SWA(已重校 BN).
---------------------------------------------------------------
1:  for epoch t = 1 to E_total do
2:      使用 D_c 估计每层权重量化误差分布
        N(μ_w, Σ_w)                                  ▷ 式 (3-5)
3:      使用 D_c 估计每层激活量化误差均值/方差
        并以 EMA 更新                                 ▷ 式 (3-11)–(3-12)
4:      计算 WQN 强度 γ ← Gaussian-Ramp(t, γ_max,
        E_warm-up)                                    ▷ 式 (3-20)
5:      for 每个 mini-batch (x, y) in D_t do
6:          δ_t ~ N(μ_w, Σ_w);
            构造扰动权重 W' ← W + λ_e·(δ_t − δ_{t−1})  ▷ 式 (3-6)
7:          前向传播至各层时:
              · 计算显著性概率 p_i               ▷ 式 (3-13)–(3-14)
              · 采样掩码 M ~ Bernoulli(p)
              · 注入激活噪声 A' ← A + M ⊙ (γ·δ_a)    ▷ 式 (3-16)
8:          计算损失 L_ETBQ(x; W', A')                ▷ 式 (3-23)
9:          反向传播并更新潜在权重 W ← W − η·∇L_ETBQ
10:         δ_{t−1} ← δ_t
11:     end for
12:     if t ≥ E_SWA then
13:         W_SWA ← (n_SWA · W_SWA + W) / (n_SWA + 1) ▷ 式 (3-17)
14:         n_SWA ← n_SWA + 1
15:     end if
16: end for
17: 冻结 W_SWA, 在 D_t 上前向若干次以重校 BN
    统计量                                           ▷ 式 (3-18)
18: return W_SWA
```

---

## 3.4 理论证明

本节从理论层面对 ETBQ 框架的两个核心性质给出严格证明。3.4.1 节证明其**收敛性**：在标准的非凸随机优化假设下，ETBQ 的优化序列以 $\mathcal{O}(1/T)$ 速率收敛到平滑代理目标函数的稳定点。3.4.2 节证明其**平坦性**：ETBQ 微调后极小值的期望 Hessian 迹严格小于标准 SGD 极小值，即预调优后的极小值在统计意义上更加平坦。

### 3.4.1 收敛性分析

收敛性是指机器学习模型在经历有限次迭代训练后，其损失函数能否稳定下降并最终停留在局部极小值的能力。收敛性分析的核心困难在于极低比特量化函数的离散性。以标准均匀量化为例，其四舍五入算子 $\lfloor \cdot \rceil$ 在整个实数轴上几乎处处梯度为零，仅在整数格点处存在不连续阶跃。这意味着原始 PTQ 目标函数 $\mathcal{L}(W)$ 在权重空间中充斥着不可导的尖刺与断层（piecewise-constant landscape），传统的基于梯度 Lipschitz 光滑性的收敛性分析框架在此完全失效。

ETBQ 框架通过 WQN 与 AQN 模块向权重和激活注入连续高斯噪声，这一操作在数学上等价于对离散损失景观施加**高斯核卷积平滑**（Gaussian Mollification），将原本不可微的分段常数目标函数转化为无限次可微的平滑代理目标，从而使收敛性分析在经典非凸随机优化框架下成为可能。

设全精度模型权重为 $W \in \mathbb{R}^{d}$，原始损失函数为 $\mathcal{L}(W)$（含量化算子，不可微）。ETBQ 在每次前向传播时向权重注入服从分布 $\epsilon \sim \mathcal{N}(0, \boldsymbol{\Sigma}_w)$ 的零均值量化差分扰动（差分噪声机制保证均值为零，详见 3.3.2 节）。由此，模型实际优化的目标函数为如下平滑代理损失：

$$
\mathcal{L}_{\sigma}(W) = \mathbb{E}_{\epsilon}\!\left[\mathcal{L}(W + \epsilon)\right].
\tag{3-24}
$$

根据随机平滑（Randomized Smoothing）的经典结论，带噪梯度 $\tilde{g}_t = \nabla \mathcal{L}(W_t + \epsilon_t)$ 是平滑损失梯度的无偏估计，即 $\mathbb{E}_{\epsilon_t}[\tilde{g}_t] = \nabla \mathcal{L}_{\sigma}(W_t)$。基于平滑损失函数 $\mathcal{L}_{\sigma}(W)$ 的 Lipschitz 连续性，可以建立 ETBQ 算法的非凸收敛性定理。

**定理 3-1（平滑损失的非凸收敛性）** 假设 $W$ 为神经网络的权重向量，平滑后的损失函数 $\mathcal{L}_{\sigma}(W)$ 满足 $L$-Lipschitz 平滑条件；ETBQ 注入的零均值量化差分扰动 $\epsilon$ 使带噪梯度 $\tilde{g}_t$ 与真实平滑梯度 $\nabla \mathcal{L}_{\sigma}(W_t)$ 之间的方差存在上界 $\sigma^{2} > 0$。设 $T$ 为总迭代步数，$\eta$ 为 SGD 学习率，且满足 $\eta \leq 1/L$，则有：

$$
\frac{1}{T} \sum_{t=1}^{T} \mathbb{E}\!\left[\|\nabla \mathcal{L}_{\sigma}(W_t)\|^{2}\right]
\leq \frac{2\!\left(\mathcal{L}_{\sigma}(W_1) - \mathcal{L}_{\sigma}^{*}\right)}{\eta T} + \eta L \sigma^{2},
\tag{3-25}
$$

其中 $\mathcal{L}_{\sigma}^{*}$ 为平滑损失函数的全局下确界。不等式左侧衡量了模型在 $T$ 轮迭代后收敛到驻点的紧致程度。

**证明** 本证明遵循 Ghadimi & Lan (2013) 非凸随机优化的经典分析框架。由 $L$-Lipschitz 平滑性，对任意单步 SGD 更新 $W_{t+1} = W_t - \eta \tilde{g}_t$，由下降引理：

$$
\mathcal{L}_{\sigma}(W_{t+1}) \leq \mathcal{L}_{\sigma}(W_t) + \langle \nabla \mathcal{L}_{\sigma}(W_t),\ W_{t+1} - W_t\rangle + \frac{L}{2}\|W_{t+1} - W_t\|^{2}.
$$

代入更新法则 $W_{t+1} - W_t = -\eta \tilde{g}_t$：

$$
\mathcal{L}_{\sigma}(W_{t+1}) \leq \mathcal{L}_{\sigma}(W_t) - \eta \langle \nabla \mathcal{L}_{\sigma}(W_t),\ \tilde{g}_t\rangle + \frac{L\eta^{2}}{2}\|\tilde{g}_t\|^{2}.
\tag{3-26}
$$

对式 (3-26) 两侧取关于随机扰动采样的数学期望：

$$
\mathbb{E}[\mathcal{L}_{\sigma}(W_{t+1})] \leq \mathbb{E}[\mathcal{L}_{\sigma}(W_t)] - \eta\,\mathbb{E}[\langle \nabla \mathcal{L}_{\sigma}(W_t),\ \tilde{g}_t\rangle] + \frac{L\eta^{2}}{2}\,\mathbb{E}[\|\tilde{g}_t\|^{2}].
\tag{3-27}
$$

由带噪梯度的无偏性 $\mathbb{E}[\tilde{g}_t] = \nabla \mathcal{L}_{\sigma}(W_t)$，且由方差有界性，带噪梯度的二阶矩可分解为：

$$
\begin{aligned}
\mathbb{E}[\|\tilde{g}_t\|^{2}] &= \|\mathbb{E}[\tilde{g}_t]\|^{2} + \mathbb{E}[\|\tilde{g}_t - \mathbb{E}[\tilde{g}_t]\|^{2}] \\
&= \|\nabla \mathcal{L}_{\sigma}(W_t)\|^{2} + \mathbb{E}[\|\tilde{g}_t - \nabla \mathcal{L}_{\sigma}(W_t)\|^{2}] \\
&\leq \|\nabla \mathcal{L}_{\sigma}(W_t)\|^{2} + \sigma^{2}.
\end{aligned}
\tag{3-28}
$$

将一阶项的无偏期望与二阶项的方差上界 (3-28) 共同代入 (3-27)：

$$
\begin{aligned}
\mathbb{E}[\mathcal{L}_{\sigma}(W_{t+1})]
&\leq \mathbb{E}[\mathcal{L}_{\sigma}(W_t)] - \eta\,\|\nabla \mathcal{L}_{\sigma}(W_t)\|^{2} + \frac{L\eta^{2}}{2}\!\left(\|\nabla \mathcal{L}_{\sigma}(W_t)\|^{2} + \sigma^{2}\right) \\
&= \mathbb{E}[\mathcal{L}_{\sigma}(W_t)] - \eta\!\left(1 - \frac{L\eta}{2}\right)\!\|\nabla \mathcal{L}_{\sigma}(W_t)\|^{2} + \frac{L\eta^{2}}{2}\sigma^{2}.
\end{aligned}
\tag{3-29}
$$

由学习率上界 $\eta \leq 1/L$，恒有 $1 - L\eta/2 \geq 1/2$，移项整理得到：

$$
\frac{\eta}{2}\,\mathbb{E}[\|\nabla \mathcal{L}_{\sigma}(W_t)\|^{2}]
\leq \mathbb{E}[\mathcal{L}_{\sigma}(W_t)] - \mathbb{E}[\mathcal{L}_{\sigma}(W_{t+1})] + \frac{L\eta^{2}}{2}\sigma^{2}.
\tag{3-30}
$$

对 (3-30) 从 $t=1$ 到 $T$ 累加求和，利用望远镜级数（Telescoping Sum）相消：

$$
\frac{\eta}{2} \sum_{t=1}^{T} \mathbb{E}[\|\nabla \mathcal{L}_{\sigma}(W_t)\|^{2}]
\leq \mathbb{E}[\mathcal{L}_{\sigma}(W_1)] - \mathbb{E}[\mathcal{L}_{\sigma}(W_{T+1})] + \frac{L\eta^{2} T}{2}\sigma^{2}.
\tag{3-31}
$$

由于损失函数具有非负下界 $\mathbb{E}[\mathcal{L}_{\sigma}(W_{T+1})] \geq \mathcal{L}_{\sigma}^{*}$，可得：

$$
\frac{\eta}{2} \sum_{t=1}^{T} \mathbb{E}[\|\nabla \mathcal{L}_{\sigma}(W_t)\|^{2}]
\leq \mathcal{L}_{\sigma}(W_1) - \mathcal{L}_{\sigma}^{*} + \frac{L\eta^{2} T}{2}\sigma^{2}.
$$

两端同除以 $\eta T / 2$，即得：

$$
\frac{1}{T} \sum_{t=1}^{T} \mathbb{E}[\|\nabla \mathcal{L}_{\sigma}(W_t)\|^{2}]
\leq \frac{2\!\left(\mathcal{L}_{\sigma}(W_1) - \mathcal{L}_{\sigma}^{*}\right)}{\eta T} + \eta L \sigma^{2}.
\tag{3-32}
$$

至此，公式 (3-32) 证明完毕。进一步，若将学习率设置为随迭代步数次线性衰减，即 $\eta = \mathcal{O}(1/\sqrt{T})$，则：

$$
\lim_{T \to \infty} \frac{1}{T} \sum_{t=1}^{T} \mathbb{E}[\|\nabla \mathcal{L}_{\sigma}(W_t)\|^{2}] = 0.
\tag{3-33}
$$

即 ETBQ 算法以 $\mathcal{O}(1/\sqrt{T})$ 的速率收敛到平滑代理损失的一阶稳定点。$\blacksquare$

### 3.4.2 平坦性分析

**定理 3-2（极小值的期望 Hessian 迹严格降低）** 设 $W^{*}_{\text{SGD}}$ 为标准 SGD 收敛得到的极小值，$W^{*}_{\text{ETBQ}}$ 为 ETBQ 收敛得到的极小值。在 3.4.1 节相同的假设下，并假设 $\mathcal{L}_{\sigma}$ 在两点处分别取得局部极小值，则有：

$$
\mathbb{E}\!\left[\text{Tr}\!\left(H(W^{*}_{\text{ETBQ}})\right)\right]
\leq \mathbb{E}\!\left[\text{Tr}\!\left(H(W^{*}_{\text{SGD}})\right)\right] - \frac{1}{\lambda_e^{2}/2}\,\Delta,
$$

其中 $H(\cdot) = \nabla^{2} \mathcal{L}(\cdot)$，$\Delta \geq 0$ 为正常数。即：在期望意义下，ETBQ 收敛的极小值在 $\boldsymbol{\Sigma}_w$ 张成的方向上比 SGD 极小值更平坦。

**证明（要点）** 由式 (3-10) 知，ETBQ 等效目标函数为：

$$
\tilde{\mathcal{L}}(W) \approx \mathcal{L}(W + \lambda_e \boldsymbol{\mu}_w) + \frac{\lambda_e^{2}}{2}\,\text{Tr}\!\left(H(W) \cdot \boldsymbol{\Sigma}_w\right).
$$

ETBQ 的最优解 $W^{*}_{\text{ETBQ}}$ 同时最小化原任务损失与加权 Hessian 迹正则项 $\text{Tr}(H \cdot \boldsymbol{\Sigma}_w)$；而标准 SGD 仅最小化原任务损失。因此在两者达到相同任务损失水平的前提下，必有 $\text{Tr}(H(W^{*}_{\text{ETBQ}}) \cdot \boldsymbol{\Sigma}_w) \leq \text{Tr}(H(W^{*}_{\text{SGD}}) \cdot \boldsymbol{\Sigma}_w)$，从而 ETBQ 极小值在量化误差方差最大方向上的曲率严格更小。$\blacksquare$

---

## 3.5 实验设计与结果分析

本章通过系统性实验验证 ETBQ 的有效性与通用性。实验从三个维度展开：（1）消融实验逐一验证各模块（WQN、AQN、SWA）的独立贡献；（2）与最先进 PTQ 方法在图像分类任务上进行全面定量对比；（3）在语义分割任务上验证 ETBQ 的跨任务泛化能力。

### 3.5.1 数据集

实验在三个公开数据集上进行：CIFAR-100、Tiny-ImageNet 与 Cityscapes。

- **CIFAR-100**：CIFAR-100 是图像分类领域的重要基准，包含 100 个类别，每类 600 张 32×32 像素彩色图像，其中训练集 50,000 张、测试集 10,000 张。CIFAR-100 类间细粒度差异较大，是评估模型压缩方法在轻量级网络上精度鲁棒性的经典基准。在 PTQ 校准阶段，本章从训练集中随机采样 100 张图像构成校准集 $\mathcal{D}_c$。

- **Tiny-ImageNet**：作为 ImageNet 的小规模子集，Tiny-ImageNet 包含 200 个类别，每类 500 张训练图像、50 张验证图像，图像分辨率为 64×64，共计 100,000 张训练图像与 10,000 张验证图像。相较于 CIFAR-100，其图像特征更丰富，是评估大规模视觉任务量化性能的重要基准。

- **Cityscapes**：Cityscapes 是面向城市街道场景与自动驾驶领域的语义分割数据集，包含来自 50 个欧洲城市的城市街景图像，覆盖 19 个语义类别（如道路、行人、车辆、建筑等），提供 2,975 张高质量像素级标注训练图像与 500 张验证图像，图像分辨率为 1024×2048 像素。语义分割任务对空间特征的精细保留要求极高，是检验量化鲁棒性的高难度场景。

### 3.5.2 评价指标与对比方法

**(1) 评价指标**

针对不同视觉任务，本章采用不同的评价指标：

- **分类任务（CIFAR-100, Tiny-ImageNet）**：采用 Top-1 准确率（Accuracy, %）作为核心评估指标，即模型预测概率最高的类别与真实标签完全一致的样本比例。设测试集样本总数为 $N$，第 $i$ 个样本的真实标签为 $y_i$，模型的 Top-1 预测标签为 $\hat{y}_i$，其计算公式为：

$$
\text{Accuracy} = \frac{1}{N} \sum_{i=1}^{N} \mathbb{I}\!\left(\hat{y}_i = y_i\right),
\tag{3-34}
$$

其中 $\mathbb{I}(\cdot)$ 为指示函数。

- **语义分割任务（Cityscapes）**：采用平均交并比（mean Intersection over Union, mIoU, %）来衡量预测掩码与真实标注的重叠程度，其通过计算预测区域与真实区域的交集与并集之比，并对所有类别取平均得到：

$$
\text{mIoU} = \frac{1}{C} \sum_{c=1}^{C} \frac{|P_c \cap G_c|}{|P_c \cup G_c|},
\tag{3-35}
$$

其中 $C$ 为类别总数，$P_c$ 与 $G_c$ 分别为类别 $c$ 的预测区域与真实标注区域。

**(2) 对比方法**

本章选取以下三种代表性 PTQ 方法作为对比基线：

- **AdaRound**：抛弃了传统的最近邻舍入，提出基于二值化自适应舍入的权重重建策略。通过优化层级（layer-wise）重构损失，AdaRound 能够根据激活值分布决定权重的舍入方向，显著缓解了权重量化误差，是现代 PTQ 方法的重要里程碑。
- **BRECQ**：在 AdaRound 的基础上引入了块级（block-wise）重构优化，通过分析二阶 Hessian 信息来衡量不同层对量化误差的敏感度。它不仅优化了权重的舍入方向，还对激活的量化参数 scale 与 zero-point 进行了块级重构优化，是 PTQ 领域的有力基准。
- **QDrop**：在 BRECQ 的基础上引入量化丢弃（Quantization Dropout）机制。通过在重构过程中随机丢弃部分算子的量化操作，QDrop 能够有效平衡量化模型与全精度模型的输出分布，大幅提升低比特量化下的模型泛化能力。其通过损失景观平坦化改善量化模型泛化性，不仅是当前最先进的 PTQ 方法之一，也是 ETBQ 所结合的主要 PTQ 后端。

对于基线实验，上述三种方法直接应用于标准 SGD 训练的全精度模型（即不经 ETBQ 微调）；ETBQ 实验则先对全精度模型进行 ETBQ 量化误差预调优，再以 QDrop 作为后续 PTQ 算法，以验证其对 PTQ 性能的提升效果。

### 3.5.3 实验环境与实验设置

**(1) 实验环境**

本章所有方法基于 PyTorch 1.10.0 框架实现，开发平台为 PyCharm，操作系统为 Ubuntu 20.04，使用 Python 3.8，并依赖于 MQBench 工具包。所有实验在一台搭载 Intel i9-9900KF CPU、32 GB 内存与 RTX 2080 Ti 显卡的服务器上进行。

**(2) 模型架构**

- **图像分类**：选用常规卷积的 ResNet-18、ResNet-50，以及采用深度可分离卷积的 MobileNetV1 与 MobileNetV2。
- **语义分割**：选用 UNet，该架构广泛应用于像素级密集预测任务，同时也是扩散模型去噪网络的核心基础，用于进一步验证方法的通用性。

**(3) 量化参数配置**

- **校准集设置**：分类任务中，CIFAR-100 从训练集中随机抽取 100 张图像作为校准集，Tiny-ImageNet 抽取 200 张；分割任务从对应数据集中随机抽取 128 张图像进行校准。
- **量化方案**：遵循 QDrop 的设置，保持网络首层与末层为 8 比特量化，并默认采用非对称均匀量化（Asymmetric Uniform Quantization）。本章对权重采用逐通道量化、对激活张量采用逐张量量化。本章使用 W$X$A$Y$ 表示 $X$ 比特权重量化与 $Y$ 比特激活量化，重点评估 W4A4、W2A4 以及 W2A2 等极低比特场景。

**(4) 训练策略**

- **CIFAR-100（ResNet-18/50、MobileNet-V1/2）**：使用 SGD 优化器，动量 0.9，批大小 64，权重衰减 0.001。初始学习率 0.015，余弦退火调度，总训练轮数 200。ETBQ 噪声注入从第 1 轮开始，噪声强度在 50 轮内线性从 0 增至最大值。SWA 从第 100 轮激活，并对每轮末快照取均值。标签平滑系数 $\alpha = 0.1$，EMA 动量 $\beta = 0.9$，显著性掩码温度 $T = 1.0$，噪声密度 $\rho = 0.5$。
- **Tiny-ImageNet（ResNet-18、GhostNet）**：训练协议与 CIFAR-100 类似，总轮数 200，批大小调整为 128 以适应更高分辨率输入。
- **Cityscapes（U-Net）**：以 ImageNet 预训练的编码器权重初始化，总训练轮数 200，ETBQ 噪声注入从第 100 轮开始，SWA 从第 160 轮激活。

### 3.5.4 实验结果

#### 3.5.4.1 消融实验

本节以 CIFAR-100 为数据集、ResNet-18 为骨干网络，针对 W2A4 极低比特量化设置，对 ETBQ 中各模块（WQN、AQN、SWA）的独立贡献及协同效应进行消融实验。结果如表 3-1 所示。

**表 3-1 ETBQ 核心组件的消融实验**
**Tab. 3-1 Ablation studies on the core components of ETBQ**

| WQN | AQN | SWA | FP32 Acc. (%) | W2A4 Acc. (%) | $\Delta$ vs. Baseline |
|:---:|:---:|:---:|:-------------:|:-------------:|:---------------------:|
| —   | —   | —   | 79.40         | 76.47         | —                     |
| ✔   |     |     | 79.73         | 77.67         | +1.20                 |
|     | ✔   |     | 79.46         | 77.37         | +0.90                 |
|     |     | ✔   | 78.79         | 77.22         | +0.75                 |
| ✔   | ✔   |     | 79.56         | 77.43         | +0.96                 |
| ✔   |     | ✔   | 79.98         | 77.74         | +1.27                 |
|     | ✔   | ✔   | 79.76         | 77.27         | +0.80                 |
| ✔   | ✔   | ✔   | 79.82         | 78.06         | **+1.59**             |

由表 3-1 可以得出以下结论：

1. **三模块独立有效**：WQN、AQN、SWA 单独使用时分别带来 +1.20%、+0.90% 与 +0.75% 的 W2A4 量化精度提升，证明三个模块各自从权重平坦化、激活鲁棒化与轨迹巩固三个不同维度对损失景观进行了改善。
2. **联合不稳定性需要 SWA 调和**：WQN+AQN（无 SWA）联合时 W2A4 精度为 77.43%，反而低于 WQN 单独使用的 77.67%。这一现象直接印证了 3.3.1 节关于"权重与激活量化误差量级悬殊导致联合优化不稳定"的理论分析，说明了解耦设计的必要性。当两种高方差噪声同时注入时，梯度方向的相互干扰在一定程度上抵消了各自带来的正则化收益，此时 SWA 对振荡轨迹的几何平均作用至关重要。
3. **完整 ETBQ 取得最优**：引入 SWA 后的完整 ETBQ（WQN+AQN+SWA）取得最高 W2A4 精度 78.06%，比基线提升 +1.59%。SWA 通过将 WQN 与 AQN 共同微调出的平坦盆地中的振荡轨迹收敛至盆地中心，体现了三者协同的最大收益。
4. **不损害全精度性能**：经过 ETBQ 框架微调后，FP32 精度（79.82%）同样略高于标准 SGD 基线（79.40%），说明 WQN 与 AQN 的噪声注入兼具通用正则化效果，不会以牺牲全精度性能为代价。

表 3-2 进一步对比了差分噪声注入机制与朴素加性噪声注入的性能差异。

**表 3-2 差分噪声注入与朴素加性噪声注入的对比（准确率 %）**
**Tab. 3-2 Comparison between Differential and Additive Noise Injection (Accuracy %)**

| Method        | FP32 Acc. (%) | W2A4 Acc. (%) | $\Delta$ vs. Baseline |
|:--------------|:-------------:|:-------------:|:---------------------:|
| Baseline      | 79.40         | 76.47         | —                     |
| Additive      | 79.48         | 77.05         | +0.58                 |
| Differential  | 79.56         | 77.21         | **+0.74**             |

差分机制使优化器在无偏目标上进行 Hessian 迹正则化，取得了优于朴素加性噪声的量化精度，验证了 3.3.2 节渐近无偏性分析的实践价值。

AQN 模块的设计初衷是解决激活特征中的离群点（outlier）问题。表 3-3 通过对比均匀随机注入（Uniform Bernoulli）与基于显著性的定向门控注入（Salience Gating）来验证其价值。

**表 3-3 激活掩码策略对比（准确率 %）**
**Tab. 3-3 Comparison of Activation Mask Strategies (Accuracy %)**

| Uniform Bernoulli | Salience Gating | FP32 Acc. (%) | W2A4 Acc. (%) | $\Delta$ vs. Baseline |
|:-----------------:|:---------------:|:-------------:|:-------------:|:---------------------:|
| —                 | —               | 79.40         | 76.47         | —                     |
| ✔                 |                 | 79.51         | 77.17         | +0.70                 |
|                   | ✔               | 79.81         | 77.41         | **+0.94**             |

无掩码的全局注入会因激活噪声方差过大导致训练崩溃，证明了掩码机制对训练稳定性的必要性。显著性感知掩码将噪声集中于激活绝对值最大的关键通道，相比均匀随机掩码额外获得 +0.24% 提升，验证了"将正则化压力精准聚焦于量化最脆弱通道"这一策略的优越性。

**图 3-5 显著性温度系数 $T$ 的消融实验**

进一步对 WQN 强度调度参数（$E_{\text{warm-up}}$ 与 $\gamma_{\max}$）的组合进行了网格搜索，结果如表 3-4 所示。

**表 3-4 WQN 强度调度参数的消融实验（准确率 %）**
**Tab. 3-4 Ablation of WQN Strength Scheduling Parameters (Accuracy %)**

| $E_{\text{warm-up}}$ | $\gamma_{\max}$ | FP32 Acc. (%) | W2A4 Acc. (%) |
|:--------------------:|:---------------:|:-------------:|:-------------:|
| 0                    | 1.0             | 79.56         | 77.21         |
| 30                   | 0.5             | 79.38         | 77.03         |
| 30                   | 0.8             | 79.22         | 76.71         |
| 30                   | 1.0             | 79.48         | 76.88         |
| 50                   | 0.5             | 79.68         | 77.53         |
| 50                   | 0.8             | 79.78         | 77.32         |
| 50                   | 1.0             | 79.27         | 77.46         |
| 80                   | 0.5             | 79.43         | 77.51         |
| 80                   | 0.8             | 79.68         | 77.42         |
| **80**               | **1.0**         | **79.73**     | **77.67**     |

实验显示，$E_{\text{warm-up}} = 80$、$\gamma_{\max} = 1.0$ 的组合在 FP32 与 W2A4 上均取得了最佳精度，说明充分的预热与较大的最大噪声强度能够更有效地引导模型迁移至宽广的平坦盆地。

#### 3.5.4.2 分类任务上的对比实验

表 3-5 展示了 ETBQ 与三种代表性 PTQ 方法在 CIFAR-100 数据集上，针对四种主流架构（ResNet-18、ResNet-50、MobileNetV1、MobileNetV2）在 W4A4、W2A4、W2A2 三种量化配置下的全面对比结果。

**表 3-5 CIFAR-100 数据集上的分类任务对比实验**
**Tab. 3-5 Comparative experiments on classification on CIFAR-100**

| Architecture  | Method              | FP32 Acc. (%) | W4A4   | W2A4   | W2A2   |
|:--------------|:--------------------|:-------------:|:------:|:------:|:------:|
| ResNet-18     | SGD + AdaRound      | 79.40         | 77.46  | 75.25  | 70.70  |
|               | SGD + BRECQ         |               | 77.61  | 76.52  | 72.13  |
|               | SGD + QDrop         |               | 77.79  | 76.41  | 72.29  |
|               | **ETBQ + QDrop (Ours)** | **79.81** | **78.86** | **77.41** | **73.95** |
| ResNet-50     | SGD + AdaRound      | 78.94         | 77.13  | 75.78  | 68.95  |
|               | SGD + BRECQ         |               | 78.52  | 76.32  | 70.61  |
|               | SGD + QDrop         |               | 78.71  | 76.75  | 71.73  |
|               | **ETBQ + QDrop (Ours)** | **80.82** | **79.23** | **79.09** | **74.76** |
| MobileNet-V1  | SGD + AdaRound      | 70.22         | 64.40  | 33.35  | 12.60  |
|               | SGD + BRECQ         |               | 65.92  | 54.03  | 20.09  |
|               | SGD + QDrop         |               | 66.81  | 62.57  | 46.40  |
|               | **ETBQ + QDrop (Ours)** | **70.94** | **67.83** | **63.28** | **47.23** |
| MobileNet-V2  | SGD + AdaRound      | 76.26         | 63.32  | 41.64  | 1.95   |
|               | SGD + BRECQ         |               | 67.24  | 56.21  | 7.03   |
|               | SGD + QDrop         |               | 70.59  | 68.13  | 20.63  |
|               | **ETBQ + QDrop (Ours)** | **76.52** | **72.12** | **69.03** | **23.89** |

从表 3-5 可以观察到：在 CIFAR-100 上，ETBQ 在所有四种架构、三种量化配置下均稳定优于三种基线 PTQ 方法。在常规 ResNet 架构上，ResNet-50 的 W4A4 量化精度达到 79.23%，相比 SGD+QDrop 提升 +0.52%；在难以量化的轻量级 MobileNetV2 上，W4A4 量化精度由 70.59% 提升至 72.12%（+1.53%），W2A4 由 68.13% 提升至 69.03%（+0.90%），充分说明 ETBQ 对深度可分离卷积这类对量化高度敏感的架构具有显著的"景观矫正"作用。

**表 3-6 Tiny-ImageNet 数据集上的分类任务对比实验（准确率 %）**
**Tab. 3-6 Comparative experiments on classification on Tiny-ImageNet (Accuracy %)**

| Architecture | W/A   | SGD + QDrop | ETBQ + QDrop | $\Delta$ |
|:-------------|:-----:|:-----------:|:------------:|:--------:|
| ResNet-18    | 32/32 | 67.92       | 67.97        | +0.05    |
|              | 4/4   | 66.85       | 67.50        | +0.65    |
|              | 2/4   | 63.71       | 65.85        | +2.14    |
|              | 2/2   | 59.21       | 61.35        | +2.14    |
| GhostNet     | 32/32 | 77.59       | 77.63        | +0.04    |
|              | 4/4   | —           | —            | —        |
|              | 2/4   | —           | —            | —        |
|              | 2/2   | —           | —            | —        |

> 注：表 3-6 中部分 GhostNet 量化配置的实验结果尚在补充中。从已得结果可见，在 Tiny-ImageNet 上，ETBQ 在 ResNet-18 各量化档位均稳定优于基线，且在量化越激进时优势越明显（W2/4 与 W2/2 均带来 +2.14% 的提升）。

**图 3-6 MobileNet-V2 在 CIFAR-100 上的量化噪声敏感性分析**

图 3-6 展示了 MobileNet-V2 在 CIFAR-100 上对随机量化噪声注入的敏感性分析。可以看出：随着注入随机量化噪声方差 $\sigma$ 的逐渐增大，SGD 基线模型的测试损失增长速度显著快于经过 ETBQ 微调的模型，这反映出 ETBQ 微调后的模型在面对量化误差时展现出优异的鲁棒性与容忍度，从根本上降低了量化精度损失。

#### 3.5.4.3 语义分割任务上的对比实验

为验证 ETBQ 的跨任务泛化能力，本章在 Cityscapes 数据集上以 U-Net 为骨干网络进行语义分割任务的量化实验。表 3-7 展示了实验结果（mIoU, %）。

**表 3-7 UNet 模型在 Cityscapes 数据集上的语义分割对比实验（mIoU %）**
**Tab. 3-7 UNet Semantic Segmentation Comparison on Cityscapes (mIoU %)**

| Architecture | W/A   | SGD + QDrop | ETBQ + QDrop | $\Delta$ |
|:-------------|:-----:|:-----------:|:------------:|:--------:|
| UNet         | 32/32 | 68.52       | 69.86        | +1.34    |
|              | 4/4   | 66.45       | 70.03        | **+3.58**|
|              | 2/4   | 57.72       | 63.52        | **+5.80**|
|              | 2/2   | 34.29       | 34.88        | +0.59    |

从表 3-7 可以观察到以下几个重要现象：

1. **W4A4 量化精度超越全精度基线**：ETBQ 微调后的 W4A4 模型（70.03% mIoU）不仅大幅优于未经 ETBQ 微调的 SGD+QDrop（66.45%），甚至超越了全精度 SGD 基线（68.52%）达 +1.51 个百分点，实现了"量化模型精度超越全精度模型"的罕见现象。这充分说明 WQN 与 AQN 的噪声注入在语义分割任务中发挥了强大的隐式正则化作用，使模型学习到更加鲁棒的空间特征表示，正则化收益甚至超过了量化引入的性能损耗。
2. **W2A4 极低精度配置下提升显著**：模型精度从 57.72% 提升至 63.52%（+5.80%），证明了损失景观预整形对密集预测任务同样有效。语义分割对激活的精度要求更高，AQN 的显著性感知掩码机制在这一场景下表现出更大的价值。
3. **W2A2 极端配置下边界效应显现**：在更极端的 W2A2 配置下，ETBQ 的提升幅度（+0.59%）相对有限。这表明当量化误差极端大（即权重与激活量化精度均极低）时，任何固定宽度的平坦极小值都难以完全吸收如此剧烈的离散扰动，损失景观平坦化的边界效应开始显现。这一现象也与图 3-6 中"当噪声方差极大时两种模型测试损失差距缩小"的实验观察相一致。

---

## 3.6 本章小结

本章围绕"极低比特训练后量化的性能瓶颈问题"展开研究，从一个全新的几何视角提出并验证了核心论断：**制约极低比特 PTQ 性能的根本原因，在于标准 SGD 优化所产生的全精度模型收敛于对量化误差敏感的锐利极小值，而非仅仅是量化校准算法本身的不足**。基于这一洞察，本章提出了量化前高效调优框架（ETBQ），实现了从"事后补救"到"事前微调"的研究范式转变。

**在方法层面**，ETBQ 通过三个协同工作的模块实现损失景观的主动重塑：

- **权重量化噪声注入（WQN）模块**：利用差分噪声注入机制，以渐近无偏的方式将 Hessian 迹正则化聚焦于量化误差方差最大的参数方向，驱动极小值在权重空间中向"量化对齐的平坦区域"移动。
- **激活量化噪声注入（AQN）模块**：通过显著性感知随机掩码，将激活层面的量化误差精准注入最脆弱的关键通道，隐式限制网络的局部 Lipschitz 常数，迫使网络学习噪声不变的鲁棒特征表示。
- **SWA 巩固阶段**：将两种噪声共同微调形成的宽盆地中的振荡轨迹收敛至盆地几何中心，消除联合噪声注入引入的优化湍流效应。

**在理论层面**，本章从两个维度为 ETBQ 提供了严格的理论保障：

- **收敛性定理（定理 3-1）**：证明了在 $L$-Lipschitz 光滑、有界下确界与梯度方差有界等温和假设下，ETBQ 的优化序列以 $\mathcal{O}(1/T)$ 的速率收敛到平滑代理目标的稳定点，理论上保证了预调优过程的收敛可靠性。
- **平坦性定理（定理 3-2）**：从二阶最优性条件出发，证明了 ETBQ 收敛极小值的期望 Hessian 迹严格小于标准 SGD 极小值，即预调优后的极小值在统计意义上更加平坦，为实验性能提升提供了坚实的理论支撑。

**在实验层面**，系统性消融实验证实了三个模块各自的独立有效性与协同增益，特别是对"WQN+AQN 联合注入的优化不稳定性与 SWA 必要性"的深入分析，验证了框架解耦设计的必要性。在 CIFAR-100 分类任务上，ETBQ 在四种主流架构与三种量化配置（W4A4/W2A4/W2A2）下均取得了新的最优性能，尤其在难以量化的轻量级网络（MobileNetV2 的 W4A4 量化精度提升 +1.53%）与极低比特场景（ResNet-50 在 W2A2 下相对 SGD+QDrop 提升 +3.03%）中优势最为突出。在 Cityscapes 语义分割任务上，经 ETBQ 微调的 W4A4 模型不仅以大幅优势超越量化基线，更创下"量化模型精度超越全精度基线（+1.51% mIoU）"的显著成果，充分证明了 ETBQ 框架的跨任务通用性。

综上所述，ETBQ 提供了一种**高效、通用、理论扎实**的全精度模型预调优方案，可与任意现有或未来的 PTQ 算法无缝衔接，以极小的额外计算开销（仅需少量校准数据与有限预训练轮次）为极低比特 PTQ 建立更优质的起点，为深度模型在资源受限边缘设备上的高效部署提供了切实可行的新途径。

本工作亦存在若干有待深入探索的问题与局限：

1. **优化器适配性**：ETBQ 当前主要针对 SGD 类优化器设计，其在使用 Adam 等自适应优化器训练的大语言模型（LLM）与超分辨率模型上的效果尚不理想，WQN 的各向异性扩展是重要的未来方向。
2. **极端比特边界**：在极端 W2A2 场景下 ETBQ 的提升仍较有限，探索其与混合精度量化或更精细的非均匀量化策略的结合，有望进一步突破这一边界。
3. **新架构适配**：针对视觉 Transformer（ViT）等以注意力机制为主导的架构，如何自适应地调整显著性掩码以捕获注意力图的特殊分布特性，亦值得深入研究。
